---
title: "Safety Beyond the Interface: Detecting Harm via Latent States in Large Language Models"
title_zh: 超越接口的安全：通过大语言模型中的潜在状态检测危害
authors: "Alizishaan Khatri, Chiquita Prabhu, Omkar Neogi"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19472v1"
tags: ["query:llm-security"]
score: 9.0
evidence: LLM潜在状态探针检测有害提示，与防护模型竞争
tldr: "大型语言模型在自主系统中广泛应用，但外部安全护栏带来延迟与计算开销，且无法洞察模型内部状态，存在保障缺口。本研究从 LLaMA-3.1-8B 提取激活，训练仅 12.6M 参数的轻量 MLP 探针检测有害提示。在 WildJailbreak、Beavertails、AEGIS 2.0 上分别取得 99%、83%、84% 的 F1 分数，性能可比千倍规模的外部护栏模型，同时大幅降低延迟和算力成本。这证明模型内部潜在状态已蕴含有害信息，可实现更高效的安全检测。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.19472v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 808, \"height\": 331, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19472v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19472v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 698, \"height\": 266, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.19472v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 608, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19472v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 640, \"height\": 195, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19472v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 762, \"height\": 409, \"label\": \"Table\"}]"
motivation: 外部护栏模型盲区且引入延迟和开销，资源受限场景难以部署，需更高效安全检测。
method: 从 LLaMA-3.1-8B 模型的隐藏层提取激活，训练一个仅含 12.6M 参数的轻量级 MLP 探针，用于对有害提示进行二分类检测。
result: "在 WildJailbreak、Beavertails、AEGIS 2.0 三个数据集上，探针的 F1 分数分别达到 99%、83%、84%，性能与千倍规模的外部护栏模型相当。"
conclusion: 该模型潜在状态已编码有害信息，探针可低成本高效检测，从而弥补外部护栏盲区。
---

## 摘要
自主系统越来越依赖大语言模型（LLMs），但围绕这些模型的安全基础设施引入了延迟和计算开销。这限制了在资源受限、时间关键部署中的实用性。现有的外部护栏模型对模型内部工作机制不可见，造成了根本性的保障缺口。我们要问：模型是否已经知道内容何时有害？我们从 LLaMA-3.1-8B 中提取激活，并训练轻量级 MLP 分类探针（1260 万参数）来检测有害提示。在 WildJailbreak、Beavertails 和 AEGIS 2.0 上评估，我们的探针分别取得了 99%、83% 和 84% 的 F1 分数，与参数规模大 1000 倍的护栏模型相比具有竞争力，同时降低了延迟和计算成本。

## Abstract
Autonomous systems increasingly rely on Large Language Models (LLMs) yet the safety infrastructure surrounding these models introduces latency and compute overhead. This limits utility in resource-constrained, time-critical deployments. Existing external guardrail models remain blind to the model's internal workings, creating a fundamental assurance gap. We ask: does the model already know when the content is harmful? We extract activations from LLaMA-3.1-8B and train lightweight MLP classifier probes (12.6M parameters) to detect harmful prompts. Evaluated on WildJailbreak, Beavertails, and AEGIS 2.0, our probes achieve F1 scores of 99%, 83%, and 84%, respectively competitive with 1000x larger guard models while cutting latency and compute costs.

---

## 论文详细总结（自动生成）

# 《Safety Beyond the Interface: Detecting Harm via Latent States in Large Language Models》详细总结

## 1. 核心问题与整体含义

- **研究背景与动机**：大语言模型（LLM）正被集成到无人机任务规划、太空操作决策支持等自主系统中，有害或失准输出不再只是质量问题，而是可靠性问题。
- **现有安全方案的不足**：
  - 构建期方案（如 RLHF 对齐训练）存在脆弱性。
  - 运行时主流方案依赖外部守卫模型（如 LLaMA Guard、ShieldGemma、WildGuard），这些模型：
    - 引入额外延迟与计算开销；
    - 无法观察主模型内部状态，形成“根本性保障缺口”；
    - 对提示注入等攻击存在盲区，攻击者可在输出被外部过滤器检查之前劫持模型内部表示。
- **核心问题**：**模型是否“已经知道”内容有害？** 论文假设 LLM 的隐藏状态中已编码安全相关信息，可以直接从内部激活中读取有害信号，从而减少对外部安全基础设施的依赖。
- **整体含义**：提出一种基于潜在状态探针的运行时防御思路，用轻量分类器直接利用主模型内部激活进行安全判定，实现更低延迟、更低算力开销的有害内容检测。

## 2. 论文提出的方法论

### 2.1 核心思想

- 从冻结的 LLaMA-3.1-8B 模型中提取激活，训练一个轻量级 MLP 分类探针，对提示是否“有害”进行二分类。
- 利用“模型内部表示已经包含安全信息”这一观察，直接在潜在状态上读取安全信号，而非依赖外部模型。

### 2.2 关键技术细节

- **激活提取**：
  - 使用 LLaMA-3.1-8B 的最后一层隐藏状态；
  - 采用 **last-token pooling**，即取最后一个 token 的隐藏向量；
  - 每个提示得到 4096 维向量。
- **分类器架构**：
  - 6 层 MLP，维度逐步递减：`4096 → 2048 → 2048 → 512 → 512 → 64 → 2`；
  - 中间层使用 GELU 激活和 Dropout；
  - 最后一层为 Softmax 输出二分类概率；
  - 总参数量 **12.6M**，仅为 LLaMA-3.1-8B 的 **0.16%**。
- **训练设置**：
  - 优化器：AdamW；
  - 学习率：\(2.5 \times 10^{-4}\)；
  - 批量大小：1024；
  - 训练轮数：50 epochs；
  - 损失函数：交叉熵。

### 2.3 算法流程（文字说明）

论文中的 Algorithm 1 可概括为两阶段：

1. **激活提取阶段**：
   - 对数据集中的每个提示 \(x_i\)，送入 LLM \(M\)；
   - 取最终隐藏状态并按 last-token pooling 得到 \(h_i \in \mathbb{R}^{4096}\)；
   - 存储 \((h_i, y_i)\)，其中 \(y_i\) 为二分类标签（1 = 有害，0 = 良性）。
2. **探针训练阶段**：
   - 初始化 MLP \(f_\theta: \mathbb{R}^{4096} \to \{0,1\}\)；
   - 多轮迭代：批量前向传播得到预测，计算交叉熵损失，反向传播更新 \(\theta\)；
   - 返回训练好的探针 \(f_\theta\)。

### 2.4 威胁模型与理论框架

- **威胁模型**：黑盒攻击者只能通过输入接口注入对抗性提示，无法访问模型权重、训练数据或中间激活；攻击目标是在满足表层过滤器约束的前提下，诱导模型生成有害输出。
- **形式化目标**：攻击者寻找 \(p^*\)，使得 \(H(M(p^*)) = 1\)，且 \(G(p, M(p)) = 0\)，其中 \(H\) 为危害分类器，\(G\) 为接口级护栏约束。
- **延迟模型对比**：
  - 外部护栏延迟：\(L_{\text{guard}} = T_{pf} + T_{llm} + T_{rf} + 2 T_{net}\)，为串行相加；
  - 探针延迟：\(L_{\text{probe}} = \max(T_{llm}, T_{probe})\)，探针可与生成并行执行，且 \(T_{probe} \ll T_{llm}\)。

## 3. 实验设计

### 3.1 数据集与场景

- **WildJailbreak**：
  - 包含 261K 提示，分为 vanilla harmful、vanilla benign、adversarial harmful、adversarial benign 四类；
  - 论文聚焦 vanilla 子集（约 100K 示例），用于分类提示意图；
  - 标签：有害 = 1，良性 = 0；按 80/10/10 分层划分训练/验证/测试。
- **Beavertails**：
  - 约 364K 英文问答对，标注 14 类危害类别及二元 `is_safe` 标志；
  - 使用其提供的 train/test 划分，以及约 700 条提示的 curated evaluation set 作为基准。
- **AEGIS 2.0**：
  - 26K 人类-LLM 交互，标注 12 类危害类别（暴力、仇恨言论、自我伤害等）；
  - 部分敏感提示在公开版本中被删减，论文通过关联 Kaggle Suicide Watch 数据集 ID 进行了重建；
  - 使用官方 80/10/10 划分；不安全 = 1，安全 = 0。

### 3.2 Benchmark 与对比方法

- **基准任务**：对给定提示进行二元有害性分类。
- **主要对比模型**（在 BeaverTails 和 AEGIS 2.0 上）：
  - BeaverDam 7B（F1：BeaverTails 89.9）
  - MD-Judge 7B（86.7）
  - GPT-4（约 1.8T 参数，86.1）
  - WildGuard 7B（84.4 / AEGIS 81.9）
  - LlamaGuard3 8B（AEGIS 77.3）
  - LlamaGuard2 8B（BeaverTails 71.8 / AEGIS 76.8）
  - Aegis-Guard 7B（BeaverTails 74.7）
- **本文探针**：12.6M 参数，BeaverTails F1 82.7，AEGIS 2.0 F1 83.5。
- 注：WildJailbreak 未与其他模型对比，因为已有工作使用的是 WildGuardTest 而非本论文的 vanilla split。

## 4. 资源与算力

- 论文**未明确说明训练所使用的 GPU 型号、数量或训练总时长**。
- 仅提及：
  - 探针运行时延迟极小：实测探针运行时间低于 **1 ms**，而典型生成时间为 50–500 ms；
  - 探针参数量为 12.6M，不需要额外 GPU 运行守卫模型，且消除了网络传输成本。
- 因此，关于训练阶段的算力开销、硬件配置、能源消耗等细节，论文未提供，无法从文本中得出。

## 5. 实验数量与充分性

- **主要实验**：
  - 3 个数据集上的探针性能评估（WildJailbreak、Beavertails、AEGIS 2.0）；
  - 与 7 个外部守卫模型在 2 个数据集上的横向对比（BeaverTails 和 AEGIS 2.0）。
- **实验充分性**：
  - 在多个公开安全基准上验证，具有一定多样性；
  - 对比了不同规模和类型的守卫模型，体现了轻量探针的竞争力；
  - 但**缺乏消融实验**：未分析不同层、不同 token 池化策略、不同 MLP 深度/宽度、Dropout 率等影响；
  - **缺乏跨模型验证**：仅在 LLaMA-3.1-8B 上测试，未验证探针是否可迁移到其他架构或规模；
  - **对比公平性存在局限**：论文自身指出 BeaverTails 评估的是响应有害性，而 AEGIS 2.0 评估的是提示分类，任务定义不完全一致；此外，不同数据集标签分布和预处理方式可能影响结果可比性；
  - 未包含统计显著性检验或多次随机种子重复实验，结果稳定性有待进一步验证。

## 6. 主要结论与发现

- **LLM 隐藏状态中确实存在可线性读取的安全信号**：轻量 MLP 探针能够在冻结的 LLaMA-3.1-8B 激活上准确分类有害内容。
- **性能结果**：
  - WildJailbreak：F1 = 99.1%（Precision 0.997，Recall 0.985）；
  - Beavertails：F1 = 82.7%（Precision 0.841，Recall 0.814）；
  - AEGIS 2.0：F1 = 83.5%（Precision 0.858，Recall 0.814）；
  - 各数据集的精确率和召回率差距在 5% 以内，表明检测性能较为稳定。
- **与大型守卫模型相比**：
  - 在 AEGIS 2.0 上超过 WildGuard（81.9%）、LlamaGuard3（77.3%）和 LlamaGuard2（76.8%）；
  - 在 BeaverTails 上与 7B 模型 WildGuard（84.4%）和 MD-Judge（86.7%）相近，优于 LlamaGuard2（71.8%）；
  - 尽管 BeaverDam（89.9%）和 GPT-4（86.1%）更高，但它们的参数量超过本文探针 1000 倍以上。
- **运行效率**：探针可与主模型前向传播并行执行，实际延迟可忽略（<1 ms），相比外部护栏的串行延迟优势明显，且支持早期终止生成。

## 7. 优点

- **方法轻量高效**：探针仅 12.6M 参数，占用资源极少，可与 LLM 同机并行运行，无需额外 GPU 或网络服务，显著降低延迟与算力成本。
- **直接利用内部状态**：规避了外部守卫模型对内部机制不可见的信息不对称问题，从攻击者无法轻易接触的激活中读取安全信号。
- **实证结果具有竞争力**：在多个公开基准上与千倍规模的外部守卫模型表现接近甚至更优。
- **支持早期干预**：可在生成过程中根据激活提前终止有害输出，减少算力浪费和用户暴露。
- **形式化威胁模型与延迟分析**：通过公式明确了攻击者约束和延迟结构，为方法定位提供了清晰理论框架。

## 8. 不足与局限

- **模型覆盖单一**：仅验证 LLaMA-3.1-8B，未测试其他模型架构或规模，跨模型迁移性未知。
- **仅使用最后一层激活**：未探索早期层或中间 token 的表示，可能遗漏不同层级的安全信息。
- **仅做二分类**：输出为二元标签，无法提供细粒度风险分数，难以满足实际部署中需要分级响应的场景。
- **缺乏对抗鲁棒性评估**：未测试探针对对抗性提示或越狱攻击的鲁棒性；攻击者可能通过构造特殊输入干扰激活表示。
- **对比不完全公平**：BeaverTails 和 AEGIS 2.0 的任务定义不同，数据集规模、类别分布和预处理方式差异可能影响结论通用性。
- **训练算力未披露**：未说明训练探针所需的 GPU 资源、训练时长和能源消耗，无法全面评估实际部署成本。
- **偏差风险**：探针可能继承底层模型或数据集中的偏见，导致对某些语言风格、文化表达或正当话题的过度过滤。
- **可解释性有限**：虽然利用内部激活，但论文未深入分析哪些特征或方向驱动分类，缺乏机制层面的解释。
- **实验重复性与统计显著性不足**：未见多次随机种子实验或置信区间，结果稳健性需进一步验证。

（完）
