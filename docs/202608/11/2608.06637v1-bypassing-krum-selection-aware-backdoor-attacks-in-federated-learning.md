---
title: "Bypassing Krum: Selection-Aware Backdoor Attacks in Federated Learning"
title_zh: 绕过Krum：联邦学习中的选择感知后门攻击
authors: "Srinivasan Subramanian, Md. Abdullah Al Hafiz Khan, Kazi Aminul Islam"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.06637v1"
tags: ["query:fl-security"]
score: 10.0
evidence: 提出绕过Krum聚合的联邦学习后门攻击
tldr: 联邦学习中，基于距离的鲁棒聚合方法（如Krum）假设良性更新聚集，但可被攻击者利用几何特性绕过。本文提出Krum-Proxy攻击，通过两阶段优化过程：首先实现后门目标，然后利用最近邻代理、随机参考建模和锚点引导对齐，生成与良性更新相似且位于聚合偏好区域内的恶意更新，并辅以投影机制保持隐蔽。在多个标准联邦学习数据集上，Krum-Proxy在保持干净精度的同时，获得了更高的后门攻击成功率，显著优于现有方法。该工作深度揭示了距离聚合对选择感知后门攻击的脆弱性，为后续鲁棒防御设计提供了重要启示。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.06637v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 851, \"height\": 934, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.06637v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 906, \"height\": 720, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.06637v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 764, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.06637v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 770, \"height\": 122, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.06637v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 260, \"label\": \"Table\"}]"
motivation: 现有距离聚合规则（如Krum）依赖良性更新成簇假设，但未考虑攻击者优化恶意更新以利用几何偏好，导致后门攻击可绕过防御。
method: 提出Krum-Proxy，采用两阶段优化分离后门注入与几何适应，利用最近邻代理、随机参考建模和锚点对齐，并通过投影约束保持隐蔽。
result: 在FedMNIST和CIFAR-10等基准上，Krum-Proxy在多种聚合规则下实现高攻击成功率且保持干净精度，显著优于基线攻击。
conclusion: 距离聚合对选择感知后门攻击存在严重脆弱性，需设计能抵抗几何适应的鲁棒防御机制。
---

## 摘要
在联邦学习中，鲁棒聚合方法被广泛用于缓解对抗性客户端行为的影响。基于距离的聚合规则，如Krum和Multi-Krum，根据良性更新形成紧密集群的假设，选择最接近多数的更新。然而，这些方法依赖于可以被自适应对手利用的几何特性。我们提出了Krum-Proxy攻击，这是一种选择感知的后门注入策略，能够持续绕过拜占庭鲁棒聚合。我们的方法不依赖简单的缩放或约束，而是主动优化恶意更新以渗透良性分布的密集核心。所提方法构造的对抗性更新不仅与良性更新相似，而且经过优化，位于聚合过程中更受青睐的更新空间区域。这通过一个两阶段优化过程实现，该过程将任务特定的攻击目标与几何感知优化分离开来，利用了最近邻代理、随机参考建模和锚点引导对齐。为了保持隐蔽性，我们引入了一个投影机制，将对抗性更新限制在现实的范数和方差界限内。在标准联邦学习基准上的实验表明，Krum-Proxy在保持干净准确率的同时实现了更高的攻击成功率，突显了基于距离的聚合在面对选择感知对手时的脆弱性。

## Abstract
Robust aggregation methods are widely used in federated learning to mitigate the impact of adversarial client behavior. Distance-based aggregation rules, such as Krum and Multi-Krum, select updates that are closest to the majority under the assumption that benign updates form a compact cluster. However, these methods rely on geometric properties that can be exploited by adaptive adversaries. We introduce the Krum-Proxy attack, a selection-aware backdoor injection strategy that consistently bypasses Byzantine-robust aggregation. Rather than relying on naive scaling or constraining, our method actively optimizes malicious updates to infiltrate the dense core of the benign distribution. The proposed method constructs adversarial updates that are not only similar to benign updates but are also optimized to lie in regions of the update space that are favored during aggregation. This is achieved through a two-stage optimization procedure that separates task-specific attack objectives from geometry-aware refinement, using a nearest-neighbor proxy, stochastic reference modeling, and anchor-guided alignment. To maintain stealth, we introduce a projection mechanism that constrains adversarial updates within realistic norm and variance bounds. Experiments on standard federated learning benchmarks show that Krum-Proxy achieves higher attack success while preserving clean accuracy, highlighting the vulnerability of distance-based aggregation to selection-aware adversaries.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：联邦学习中，客户端上传的模型更新可能被恶意篡改，产生模型投毒或后门攻击。服务器通常采用基于距离的拜占庭鲁棒聚合规则（如 Krum、Multi-Krum）来筛选“最靠近多数”的更新，假设良性更新会形成一个紧密的集群。
- **核心问题**：这类防御依赖于更新的几何分布，自适应攻击者可以利用此特性，专门优化恶意更新，使其既携带后门又位于聚合规则偏好的密集区域，从而绕过防御。
- **整体含义**：论文指出，单纯依赖单轮距离度量的 Krum 系列方法在面对“选择感知”的对手时非常脆弱。提出的 Krum‑Proxy 攻击通过显式模拟聚合器的选择偏好，实现了高效、稳定的后门注入，同时保持模型的主任务准确率。

## 2. 论文提出的方法论

- **核心思想**：攻击者不直接获取其他客户端的真实更新，而是基于本地可控数据构造“随机参考更新”来近似良性更新的几何分布，然后优化一个可微的 Krum 得分代理，使恶意更新既实现后门目标，又在更新空间中位于最可能被選中的区域。
- **关键技术细节**：
    - **随机参考构造**：攻击者在每一轮使用本地数据的不同分区、随机学习率和随机本地轮数训练多个“干净”的参考更新，以捕捉良性更新的异质性。同时基于这些参考计算质心、散布和范数中位数，并构造一个混合锚点（由两个得分最低的参考更新按 0.85:0.15 混合得到）作为几何对齐目标。
    - **Krum 代理损失**：真实 Krum 得分需要知道所有客户端的更新，攻击者用参考更新集替代：对候选恶意更新，计算它与所有参考更新的距离，取最近 k 个邻居中距离最小的 ⌊k/2⌋ 个的平均距离作为代理得分。该设计聚焦最密集的参考区域，且可微分，便于联合优化。
    - **两阶段优化**：
        1. **阶段一（后门注入）**：仅用交叉熵损失，在干净样本和触发样本混合批次上训练 $E_1$ 个 epoch，使后门任务单独收敛，避免几何损失信号的干扰。
        2. **阶段二（几何塑形）**：学习率减半，训练 $E_2$ 个 epoch，联合优化四项损失 = $\lambda_l\, L_{CE} + \lambda_n\, L_{norm} + \lambda_k\, L_{krum} + \lambda_a\, L_{anchor}$，分别对应后门保持、范数匹配（对齐良性更新范数）、代理 Krum 距离最小化、锚点对齐。
    - **投影式隐蔽性保障**：阶段二后执行两步投影：
        1. 若恶意更新偏离得分最优的参考更新超过一个标准差，则向其插值（最多拉回 75%），以维持在该参考附近。
        2. 将更新范数缩放至良性范数中位数（下限为本地良性更新范数的 0.5 倍），防止弱化后门信号。
- **算法流程**：详见论文中的 **算法 1**。关键步骤：构建参考集与锚点 → 阶段一纯后门训练 → 阶段二几何联合优化 → 投影调整。

## 3. 实验设计

- **数据集与场景**：
    - 主要数据集：CIFAR‑10、MNIST、EMNIST。
    - 联邦设置：100 个客户端，每轮随机选 10 个参与；数据分区用 Dirichlet 分布（α=0.3～0.9）模拟不同非独立同分布（non‑IID）程度。
    - 模型：轻量 Tiny ResNet‑18（约 0.27M 参数）。
- **对比的聚合规则**：
    - 脆弱基准：FedAvg。
    - 拜占庭鲁棒：Krum、Multi‑Krum。
    - 附加测试：Trimmed Mean（验证迁移性）。
- **对比的攻击方法**：
    - Scaled Backdoor（简单缩放的后门攻击）。
    - Constrain‑and‑Scale（经典限制范数与方向的隐蔽攻击）[3]。
- **评价指标**：
    - 干净样本主任务准确率（MTA）。
    - 触发样本攻击成功率（ASR）。
    - 恶意更新被选中次数（Krum）或包含比例（Multi‑Krum）。
    - 后收敛稳定性（ASR >90% 后的 MTA 崩溃次数等）。

## 4. 资源与算力

- 论文正文未明确给出 GPU 类型、数量或总训练时长。仅提到一處关于运行时开销的对比：“Krum‑Proxy 在 CIFAR‑10 的 Krum 设置下端到端运行时间是 Constrain‑and‑Scale 的 1.29 倍”。其余算力信息缺失。

## 5. 实验数量与充分性

- **主要实验**：对三种聚合方法（FedAvg、Krum、Multi‑Krum）在 CIFAR‑10 上训练 100 轮，报告 ASR 和 MTA 曲线以及最终数值（表 I）。
- **消融实验**：在 Krum 下，移除关键部件（两阶段变为单阶段、去掉代理 Krum 损失、去掉锚点+投影、使用全 k 邻居代替半 k、替换随机参考为单确定性参考），记录平均 MTA/ASR 及后收敛稳定性（表 III）。
- **异质性敏感性**：测试 α=0.7、0.5、0.3 三种 Dirichlet 浓度参数下的攻击性能。
- **跨数据集泛化**：在 MNIST 和 EMNIST 上复现主要结果。
- **聚合迁移性**：额外测试 Trimmed Mean 下的有效性。
- **恶意客户端数量影响**：分别使用 1、2、3 个恶意客户端在 Krum 和 Multi‑Krum 下测试。
- **结论客观、公平**：所有对比攻击使用相同的客户端采样、恶意预算、投毒比例、攻击起始全局模型，消融细致，基本覆盖了方法的关键设计。但算力成本未明，且仅在轻量 ResNet 和三种中小型图像数据集上测试，模型与任务多样性有限。

## 6. 论文的主要结论与发现

- Krum‑Proxy 在 Krum 和 Multi‑Krum 下均能快速达到高 ASR（接近 100%），同时保持与干净聚合相近或稍高的 MTA，显著优于 Scaled Backdoor 和 Constrain‑and‑Scale。
- 攻击有效提升了聚合层的恶意包含率：Krum 选中恶意更新的轮数从 9 增加到 29，Multi‑Krum 的恶意包含比例从 20.7% 提高到 58.3%。
- 消融表明，两阶段分离、随机参考构造、半 k 代理损失、锚点对齐与投影机制对稳定性和攻击成功率都有显著贡献，缺少任一项会导致收敛延迟、ASR 下降或严重的主任务准确率崩溃。
- 对数据异质性（不同 α）、不同数据集（MNIST、EMNIST）以及 Trimmed Mean 等其他聚合规则均表现出较强的泛化能力。
- 揭示了基于距离的鲁棒聚合在选择感知攻击下的根本性脆弱性，建议未来防御应结合时序一致性或更新结构性分析等多维信号。

## 7. 优点

- **攻击设计贴合防御机制**：深刻理解 Krum 的最近邻选择逻辑，将“被选中”概率显式建模为可优化目标，而非仅仅模仿统计特性。
- **无需访问真实更新**：通过本地随机参考构建和代理得分，实现了灰盒攻击，更贴近现实威胁模型。
- **两阶段解耦优化**：将后门收敛和几何适应分离，有效防止梯度竞争，提升了攻击的稳定性和收敛速度。
- **全面的评估体系**：不仅关注最终 ASR 和准确率，还统计了选择次数、包含比例以及后收敛稳定性，消融实验细致，证实了各组件的必要性。
- **跨设置泛化验证**：展示了在多种聚合规则、数据集和异构程度下的鲁棒性，增加了结论的可信度。

## 8. 不足与局限

- **算力成本未披露**：未报告 GPU 型号、数量、训练耗时估计，仅提及相对开销 1.29×，难以评估实际计算可行性。
- **模型与任务范围有限**：仅在小规模 ResNet（0.27M 参数）和三种标准图像数据集上测试，未涉及更大模型、语言模型或其他联邦学习任务（如 NLP），结论普适性存疑。
- **依赖本地参考质量**：在极端非 IID 或数据严重不足时，本地参考可能无法真实反映全局良性分布，攻击效能可能下降（文中仅一笔带过，未深入实验）。
- **对坐标式聚合规则未专门优化**：方法专为 Krum 式距离选择设计，对 Median、Trimmed Mean、Bulyan 等规则虽然测试有效，但未提供针对性的理论或设计解释。
- **投影步骤的阈值依赖**：投影中的超参数（如插值上限 0.75、缩放下限 0.5）需要预先设定，对不同任务可能需要调整。
- **防御演进风险**：如果防御方引入历史一致性检查、更新结构分解等，攻击效果可能被削弱，论文未对这些潜在防御展开讨论和实验。

（完）
