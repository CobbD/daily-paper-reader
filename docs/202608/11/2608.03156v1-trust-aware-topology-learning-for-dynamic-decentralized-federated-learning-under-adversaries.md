---
title: Trust-Aware Topology Learning for Dynamic Decentralized Federated Learning under Adversaries
title_zh: 对抗环境下动态去中心化联邦学习中的信任感知拓扑学习
authors: "Shubham Vaishnav, Murtaza Rangwala, Ali Beikmohammadi, Sindri Magnússon, Rajkumar Buyya"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03156v1"
tags: ["query:byzantine"]
score: 10.0
evidence: 通过信任感知拓扑学习防御去中心化联邦学习中的拓扑和模型投毒
tldr: "动态去中心化联邦学习中，敌手可同时毒化模型更新与拓扑信息，导致传统方法崩溃。本文提出DMTT协议，通过信任感知拓扑学习，利用多源信任评估动态筛选可靠合作者进行个性化聚合。在UCI HAR与PAMAP2上，面对10%至80%敌手比例，DMTT准确率分别维持在0.862和0.829以上，远优于FedAvg及鲁棒聚合器，且拜占庭影响被有效归零。该方法是唯一始终超越本地基线的方案，显著提升了抗攻击鲁棒性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03156v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1843, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03156v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 124, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03156v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1874, \"height\": 1650, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03156v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 614, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03156v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03156v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 782, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03156v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 1071, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03156v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 933, \"height\": 324, \"label\": \"Table\"}]"
motivation: 动态移动去中心化联邦学习易受模型更新和拓扑信息双重毒化攻击，现有聚合策略迅速失效，亟需信任感知的鲁棒防御。
method: DMTT构建置信加权局部拓扑，融合链路可靠性、签名声明、见证证实与Beta信任，用复合分数筛选合作者，将拜占庭影响限制在有界残差内。
result: "UCI HAR和PAMAP2上，10-80%敌手下DMTT准确率超0.862和0.829，FedAvg及鲁棒聚合器崩溃，拜占庭影响归零。"
conclusion: DMTT是唯一在所有敌手比例下均超越本地基线的去中心化联邦学习方法，有效抵御拓扑与模型毒化，实际运行验证了强鲁棒性。
---

## 摘要
在动态移动去中心化联邦学习（DFL）中，对手可以毒化模型更新以及设备用于选择协作者的拓扑信息。我们提出DMTT（具有可信拓扑的动态MURMURA），这是一种基于MURMURA的去中心化个性化联邦学习协议，利用证据深度学习来降低分布不匹配的对等体的权重，并在拓扑操纵攻击下扩展到随时间变化的图。每个设备维护一个置信度加权的本地拓扑视图，该视图源自链路可靠性估计、签名拓扑声明、见证协证和Beta分布源信任模型，然后仅通过一个融合模型兼容性、拓扑信任和链路可靠性的组合评分，在经信任筛选的协作者集上进行聚合。我们证明，筛选后的混合矩阵将拜占庭影响限制在一个有界残差δ_max内，该残差在完美筛选下消失，并实现了DMTT作为一个无协调器的分布式系统，每个客户端作为独立的ZeroMQ进程运行，通过共享的墙钟纪元同步。在UCI HAR和PAMAP2数据集上，每个数据集在100个具有Dirichlet异构性的移动客户端上进行划分，DMTT在所有测试的敌手比例（10%到80%）下，诚实节点准确率保持在0.862（UCI HAR）和0.829（PAMAP2）以上，在低敌手比例下几乎匹配无攻击准确率，并在极端情况下优雅地降至仅本地性能；静态和动态FedAvg在每个比例下都崩溃至随机水平，鲁棒聚合器（Krum、BALANCE、UBAR）未能持续超越仅本地基线，而DMTT是唯一在所有数据集和所有比例下均超越这一基线的方法，幸存的拜占庭聚合权重在整个过程中经验为零，与δ_max=0一致。该协议通过墨尔本研究云上的无协调器ZeroMQ后端在真实节点上端到端运行。

## Abstract
In dynamic mobile decentralized federated learning (DFL), adversaries can poison both model updates and the topology information devices use to choose collaborators. We present DMTT (Dynamic MURMURA with Trusted Topology), a decentralized personalized FL protocol built on MURMURA, which uses evidential deep learning to down-weight distribution-mismatched peers, extended here to time-varying graphs under topology-manipulation attacks. Each device maintains a confidence-weighted local topology view from link-reliability estimates, signed topology claims, witness corroboration, and a Beta-distributed source-trust model, then aggregates only over a trust-screened collaborator set using a composite score fusing model compatibility, topology trust, and link reliability. We prove the screened mixing matrices confine Byzantine influence to a bounded residual $δ_{max}$ that vanishes under perfect screening, and implement DMTT as a coordinator-free distributed system with each client running as an independent ZeroMQ process synchronized by a shared wall-clock epoch. On UCI HAR and PAMAP2, each partitioned across 100 mobile clients with Dirichlet heterogeneity, DMTT sustains honest-node accuracy above 0.862 (UCI HAR) and 0.829 (PAMAP2) across all tested adversary fractions (10 to 80%), nearly matching no-attack accuracy at low fractions and degrading gracefully toward local-only performance at extremes; static and dynamic FedAvg collapse to chance at every fraction, and robust aggregators (Krum, BALANCE, UBAR) fail to consistently beat a local-only baseline, while DMTT is the only method that clears this bar across both datasets at all fractions, with surviving Byzantine aggregation weight empirically zero throughout, consistent with $δ_{max}$=0. The protocol runs end-to-end on real nodes via a coordinator-free ZeroMQ backend on the Melbourne Research Cloud.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心挑战：动态去中心化联邦学习（DFL）中的双重威胁。**
    在移动物联网等场景下，设备连接拓扑随时间动态变化（移动性、故障等）。恶意节点（拜占庭对手）可以同时发起两种攻击：
    1.  **模型毒化攻击**：发送有毒的模型更新，破坏全局模型精度。
    2.  **拓扑操纵攻击**：伪造网络连接拓扑声明（如谎报邻居、隐藏链路），诱导诚实节点与恶意节点合作或孤立良性节点。
- **现有方法局限**：
    - **传统鲁棒聚合方法（如Krum, BALANCE）**：仅在固定、已知的拓扑下过滤模型更新，无法应对拓扑信息本身的欺骗，一旦拓扑视图被污染，它们仍可能筛选出错误模型。
    - **动态拓扑学习方法**：虽然能处理拓扑变化，但假设所有节点都诚实地报告拓扑信息，不适用于拜占庭攻击环境。
- **研究目标**：设计一种**信任感知的动态拓扑学习与聚合协议**，使每个节点能独立维护置信度加权的局部拓扑视图，并在此基础上筛选可信且有用的合作者，以在动态拓扑和双重攻击下实现**安全、个性化**的联邦学习。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **方案名称与基础**：**DMTT**，基于「MURMURA」（一种利用证据深度学习评估对等模型兼容性的去中心化个性化联邦学习协议），扩展至动态拓扑和拜占庭攻击场景。
- **核心思想 - 三层分离架构**：将协作决策划分为三层，逐级筛选：
    1.  **直接可达性层**：通过链路确认（ACK）、信标等机制直接检测哪些对等点是物理上可达的，并维护链路可靠性评分 \hat{p}_{ij}。
    2.  **拓扑信任层**：不直接信任接收到的拓扑声明，而是通过多种源验证每条声明，更新维护一个“受信任的局部拓扑视图”。这包括：
        - 签名新鲜度与来源验证。
        - 见证加权机制：对不同来源的声明赋予不同权重（考虑了来源的信任值 T^{topo}_{ij} 和跳数惩罚），汇总为“支持/反对”证据，计算边缘置信度 χ_i(e)，仅在证据充分时接受拓扑变更。
        - 贝塔分布信任模型：利用贝塔分布跟踪每个来源 j 的长期声明可靠性。更新贝塔先验参数（α, β），考虑直接确认、来源证实、新鲜度一致性等正面事件和矛盾、刷屏等负面事件。最终拓扑信任得分是声誉均值减去认知不确定性惩罚：T^{topo,+1}_{ij} = R_{ij} * exp(-η * U_{ij})。
    3.  **协作图层**：仅对满足“可达”和“拓扑可信”的节点，再评估其**模型兼容性** s^{model}_{ij}（利用MURMURA的证据深度学习，评估对等模型在本地数据上的准确率和认知不确定性），并结合通信成本计算最终协作分数，在预算内选择分数最高的 B 个节点进行聚合。
- **理论保证**：证明了筛选后的混合矩阵将拜占庭影响限制在一个有界残差 δ_max 内，并且在完美筛选下（即所有恶意节点都被排除），该残差为零（δ_max = 0）。同时提供了在非完美筛选下的收敛边界分析。
- **分布式实现**：将 MURMURA 重写为**无协调器的物理分布式系统**。每个客户端进程通过 ZeroMQ 进行 P2P 通信，通过**共享的墙钟纪元**同步轮次（避免单点故障），并利用确定性共享种子的随机游走模型生成一致的动态图 G_t。

### 3. 实验设计

- **数据集与场景**：
    - **UCI HAR**（人类活动识别，6类）。将 7352 个训练实例以 Dirichlet 浓度 α=0.5 划分给 N=100 个客户端，形成真实非独立同分布（non-IID）数据分布。
    - **PAMAP2**（更丰富的身体传感器活动数据，12类）。同样以 α=0.5 分配给 100 个客户端，使用原始时间窗口和更细粒度分类，用于测试模型泛化性。
    - **模型**：每个客户端训练一个三层（256-128）的感知机，搭配证据深度学习输出头。
- **对比基准**：
    - **非鲁棒基线**：FedAvg（静态图和动态图），即直接对所有邻居的模型进行平均。
    - **鲁棒聚合器**：Krum（选距离最近）、BALANCE（自适应距离过滤）、UBAR（两阶段距离+损失过滤）。这些方法均不包含拓扑信任验证。
    - **安全下界**：Local-only（仅本地训练，完全不合作）。
- **攻击配置**：
    - **攻击者比例**：从 10% 到 80% 的节点是恶意的。
    - **攻击类型**：组合攻击，即**拓扑欺骗攻击**（伪造声明仅包含其他恶意节点）和**高斯模型毒化攻击**（噪声 σ=10）同时发起。
- **超参数搜索**：对 DMTT 的遗忘因子（λ）、链路可靠性平滑率（ρ）、拓扑信任门限（τ_trust）和协作预算（B）进行了敏感性分析。

### 4. 资源与算力

- **未明确提及**：文中没有具体说明训练所使用的 GPU 型号、数量以及单次实验或完整的超参数敏感性分析耗时。

### 5. 实验数量与充分性

- **实验组数充足**：
    - **2个数据集** × **8种敌手比例** × **3个随机种子** × **4种主方案（FedAvg-static， FedAvg-dynamic， Local-only， DMTT）+ 3种鲁棒聚合器（Krum， BALANCE， UBAR）**。此外还包括对 **4个核心超参数** 的敏感性分析。
- **实验设计的充分性与客观性**：
    - **充分性**：覆盖了从低到极高敌手比例的完整威胁谱系，并在不同类型的数据集上进行了验证，评估了收敛性和最终准确率，实验设计全面。
    - **客观性**：将方法与多个公认的强基准和仅本地训练的下界进行对比，清晰揭示了其它方法在动态和欺骗场景下的失效，对比公平。分布式和仿真后端的验证也增加了结果的可靠性。敏感性分析表明默认参数性能稳定，非偶然调参结果。

### 6. 论文的主要结论与发现

- **DMTT 是唯一在所有敌手比例下显著有效的方案**。在所有测试比例（10-80%）下，DMTT 的诚实节点准确率（UCI HAR 上 ≥ 0.862， PAMAP2 上 ≥ 0.829）均**持续超越** FedAvg、所有鲁棒聚合器以及「仅本地训练」基线。
- **传统方法崩溃**：静态和动态 FedAvg 在所有比例下都崩溃到随机水平。Krum 和 BALANCE 分别在 40-60% 和 10% 敌手比例下失效，UBAR 则在 50% 以上崩溃。这些方法无法处理拓扑欺骗带来的恶意合作者筛选问题。
- **安全降级特性**：当敌手比例极高时，DMTT 性能优雅地退化为近似「仅本地训练」的水平，而非崩溃，展现出可靠的安全性。
- **完美的拜占庭影响筛除**：实证结果显示，在所有运行中，恶意节点在聚合中的权重（δ_max）均为 0，与理论分析「完美筛选」的条件一致。

### 7. 优点

- **创新性强**：系统性地解决了传统去中心化联邦学习忽略的“拓扑信息不可信”问题，其「三层独立决策」思想明确，实现了拓扑信任和模型有用性的解耦。
- **理论与实证高度统一**：理论分析证明拜占庭影响在有界残差内，并会在完美筛选下被彻底消除，而实验恰好观测到了 δ_max=0 的结果，论证严密。
- **系统设计务实**：提出的无协调器分布式架构（墙钟同步， ZeroMQ P2P）真实可部署，避免了中心化瓶颈，并已在真实计算集群上得到验证。
- **性能卓越且健壮**：在极其严苛的敌手比例（最高达 80%）下，不仅能防御攻击，还能从中获益，展现了极强的鲁棒性和安全性，且对超参数不敏感。

### 8. 不足与局限

- **理论分析假设**：完美筛选条件下的收敛性证明依赖于「双重随机混合矩阵」假设，而实际中可能使用非对称权重，需要额外的 push-sum 等技术去保证，增加了复杂性。
- **去中心化架构下的先决条件**：分布式系统中所有客户端都依赖共享种子生成一致的动态图，这是一个强同步和共享信息的前提，在实际物理部署中可能会存在挑战。
- **计算开销**：每个客户端每一轮都需要对所有可达且可信的邻居的模型进行前向传播，以评估模型兼容性，其计算开销与可达邻居数成正比，这对资源受限的边缘设备可能是一大负担，论文未就此深入讨论代价。
- **未评估的攻击类型**：仅验证了针对拓扑的“遗漏攻击”和针对模型的“高斯噪声”攻击，未讨论更复杂的、针对信任机制的长期欺骗策略（如先建立信任再背叛的共谋攻击）的效果。
- **算力与训练耗时信息缺失**：未报告实验所需的 GPU 型号、数量或训练时间，使得无法估算该方案在大规模部署下的实际计算成本和可行性边界。

（完）
