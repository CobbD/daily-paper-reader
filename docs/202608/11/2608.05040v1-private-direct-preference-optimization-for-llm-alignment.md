---
title: Private Direct Preference Optimization for LLM Alignment
title_zh: 用于大语言模型对齐的隐私保护直接偏好优化
authors: "Yangfan Jiang, Fei Wei, Ergute Bao, Xiaokui Xiao, Yaliang Li, Bolin Ding"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.05040v1"
tags: ["query:llm-security"]
score: 9.0
evidence: 形式化DPO的偏好隐私，在LLM对齐训练期间保护响应间的相对偏好
tldr: 直接偏好优化（DPO）利用人类偏好数据对齐大型语言模型（LLM），但偏好标注可能泄露种族、政治等敏感属性。现有差分隐私方法因DPO数据的特殊结构而注入冗余噪声，损害模型质量。本文提出“偏好隐私”，一种仅保护相对偏好的标签差分隐私概念，并开发PrivDPO方法。关键发现是：相邻样本（仅偏好不同）的梯度差仅沿文本决定的偏好轴，隐私保护只需沿此轴随机重缩放目标函数，无需计算逐样本梯度。这在三个对齐基准和三个LLM家族上的实验证明，PrivDPO在强隐私保证下，性能显著优于标准DP-SGD和DP-DPO，实现了迄今最佳的隐私-效用平衡，为大规模、保护隐私的LLM对齐铺平道路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05040v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 783, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05040v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1620, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05040v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1599, \"height\": 357, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05040v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 866, \"height\": 301, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05040v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1803, \"height\": 729, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1555, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 780, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 704, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 722, \"height\": 432, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 773, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 773, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 708, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1550, \"height\": 506, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05040v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1548, \"height\": 505, \"label\": \"Table\"}]"
motivation: 偏好信号可能泄露用户敏感属性（如种族、政治倾向），而现有差分隐私方法未能利用DPO中仅偏好不同的相邻示例具有的特殊梯度结构，导致过多噪声和效用下降。
method: 定义偏好隐私（label-DP风格），设计PrivDPO算法：利用相邻样本梯度差仅沿一维偏好轴的特性，通过无偏随机重缩放目标函数注入校准噪声。
result: 在三个对齐基准和三个LLM家族上，PrivDPO在不同隐私预算下始终优于DP-SGD和DP-DPO基线，实现显著的隐私-效用权衡提升。
conclusion: PrivDPO为LLM对齐提供了一种结构感知的隐私保护方案，无需逐样本梯度操作即可实现强隐私，适用于大规模训练与部署。
---

## 摘要
直接偏好优化（DPO）现已成为利用人类偏好数据对齐大语言模型（LLM）的标准方法。每个DPO示例包含一个提示和一对候选模型回复。尽管提示和回复通常是公开的或由模型生成，但回复之间的相对偏好反映主观判断，可能泄露标注者或最终用户的敏感属性。现成的隐私保护方法并不完全适配这一结构，导致在训练中引入不必要的噪声和偏斜更新。本文中，我们形式化定义了偏好隐私，这是一种针对DPO的标签差分隐私式隐私概念，仅保护候选回复间的相对偏好，并假设敌手已获知提示和回复。我们进而设计PrivDPO，一种在强制偏好隐私的同时兼容大规模LLM训练的DPO变体。我们的主要观察是：对于仅在偏好信号上不同的邻近示例，其梯度差异位于一个仅由文本决定的一维偏好轴上；所有偏好信息均流经此轴。PrivDPO通过无偏随机缩放DPO目标函数，仅沿该轴添加校准随机性，避免了逐样本梯度操作。我们在三个对齐基准和三类LLM上的实验表明，相较于隐私保护基线，PrivDPO持续实现了强大的隐私-效用权衡。

## Abstract
Direct preference optimization (DPO) is now a standard method for aligning large language models (LLMs) using human preference data. Each DPO example contains a prompt and a pair of candidate model responses. While prompts and responses are often public or model-generated, the relative preference between responses reflects subjective judgments and can reveal sensitive attributes of annotators or end users. Off-the-shelf privacy-preserving approaches are not well matched to this structure, leading to unnecessary noise injection and biased updates in training.   In this paper, we formalize preference privacy, a label-DP-style privacy notion for DPO that protects only the relative preference between candidate responses, assuming an adversary who already knows the prompt and responses. We then design PrivDPO, a DPO variant that enforces preference privacy while remaining compatible with large-scale LLM training. Our main observation is that, for neighboring examples differing only in their preference signal, the gradient difference lies on a one-dimensional preference axis determined solely by the text; all preference information flows through this axis. PrivDPO adds calibrated randomness only along this axis via an unbiased randomized rescaling of the DPO objective, avoiding per-example gradient operations. Our experiments on three alignment benchmarks and three LLM families show that PrivDPO consistently achieves strong privacy-utility trade-offs compared with privacy-preserving baselines.

---

## 论文详细总结（自动生成）

好的，以下是对论文 “Private Direct Preference Optimization for LLM Alignment” 的结构化深入总结。

### 1. 核心问题与整体含义

*   **研究背景**：直接偏好优化（DPO）是当前对齐大语言模型的主流方法之一，它通过人类标注的偏好数据（提示、好回复、坏回复）训练模型。然而，这种训练方式带来了隐私风险。
*   **核心问题**：DPO训练数据中的**偏好信号**（即人类标注者在两个候选回复中选择了哪一个）可能泄露标注者的主观判断和敏感属性，如政治倾向、宗教信仰等，可能引致歧视或骚扰。
*   **现有方法的局限**：传统的差分隐私方法（如DP-SGD）试图保护整个训练三元组（提示，回复A，回复B），但通常提示和回复文本是公开的或由模型生成，并非敏感信息。这种“过度保护”导致模型梯度空间中注入了大量不必要的噪声，严重损害了模型的对齐性能。另一种方法（如随机化响应RR）直接翻转偏好标签，虽匹配了隐私粒度，但在DPO优化中会引入偏差，同样降低效用。
*   **整体含义**：本文旨在提出一个更精确、高效的隐私保护框架，专门针对DPO场景中真正敏感的“偏好”信号进行保护，从而在提供强有力隐私保证的同时，尽可能维持模型的性能，实现更好的隐私-效用权衡。

### 2. 方法论

*   **核心思想：形式化“偏好隐私”**
    *   该定义属于标签差分隐私的范畴。它假设敌手已经完全知晓提示和两个候选回复的文本内容，唯一的目标是推断出哪个回复是被偏好的。
    *   定义为：对于两个仅在偏好上相反的`DPO`示例，算法`M`的输出应满足ε-不可区分性。这比保护整个三元组的传统差分隐私更为松弛和精确。

*   **关键技术细节：利用“偏好轴”的几何结构**
    *   **核心观察**：对两个仅偏好相反的训练样本，它们的梯度差值总是落在参数空间中的一个**一维子空间**上，这个子空间的方向由`偏好轴向量` v = ∇log π(y_w|x) - ∇log π(y_l|x) 决定，而该向量完全由非敏感的文本内容决定。
    *   **隐私含义**：偏好信息只在“偏好轴”这一个方向上泄露。因此，只需沿这个轴进行扰动，就能实现隐私保护，无需像DP-SGD那样在全参数空间添加各向同性噪声。

*   **算法流程：PrivDPO**
    *   **无偏随机重缩放机制**：PrivDPO不直接扰动梯度或原始标签，而是对DPO损失函数进行随机重缩放。
    *   **计算偏好强度** `ψ`：对于每个训练样本，计算一个标量值 `ψ`（sigmoid函数输出），它表示当前模型相对于好回复更偏好坏回复的程度。
    *   **生成随机权重** `ŵ`：设计了一个无偏的扰动机制。它以一个较高的概率（`e^ε/(e^ε+1)`）将目标函数乘以一个略大于1的权重，以一个较低的概率（`1/(e^ε+1)`）乘以一个略小于1（甚至可能为负）的权重。这种不对称采样的参数经过精确校准，使得**权重的期望值为1**，确保了梯度的无偏性。
    *   **等价于梯度扰动**：上述对目标函数的随机重缩放，在数学上等价于沿着偏好轴 `v` 对梯度进行了无偏且保护隐私的扰动，但避免了在工程上显式操作和存储巨大的逐样本梯度。
    *   **隐私化监督微调（PrivSFT）**：为了避免DPO前的SFT阶段泄露偏好信息，论文提出PrivSFT，即**同时**使用偏好和非偏好的回复进行微调，损失函数为两者的平均负对数似然，这样就不会引入任何偏好隐私成本。

### 3. 实验设计

*   **数据集**：实验使用了三个被广泛使用的标准对齐基准数据集：
    *   **Anthropic-HH**：关于对话有帮助性和无害性的数据集。
    *   **TL;DR Summarization**：基于Reddit帖子的摘要偏好数据集。
    *   **UltraFeedback-Binarized**：关于指令遵循、真实性等反馈的二值化数据集。
*   **评估指标**：
    *   **奖励指标**：奖励边际（Reward Margin）和奖励准确率（Reward Accuracy），用于评估模型对偏好和非偏好回复的区分度。
    *   **生成质量指标**：采用“LLM-as-a-judge”的评估流程，用GPT-4.1作为裁判，比较不同模型生成回复的胜率（Win Rate），评估维度包括有帮助性、无害性、简洁性等。
*   **对比方法**：
    *   **非隐私基线**：标准DPO。
    *   **隐私保护基线**：DP-SGD（代表保护整个三元组的差分隐私方法）、随机化响应（RR，代表直接扰动标签的方法）、Duchi和Piecewise机制（代表其他无偏标量扰动方法，作为消融对比）。

### 4. 资源与算力

*   **计算资源**：所有实验均在单台配备**8块NVIDIA H20 GPU**（每块96GB显存）的机器上进行。
*   **训练框架**：采用PyTorch的完全分片数据并行技术进行高效的大规模训练。
*   **训练效率**：论文展示了PrivDPO相较于标准DPO，仅引入了**可忽略不计的额外训练时间**开销。
*   **模型规模**：实验覆盖了从3B到32B参数规模的模型。

### 5. 实验数量与充分性

*   **模型覆盖**：实验在来自Qwen2.5， Llama3， Pythia等三种主流架构的多个不同规模（2.8B到32B）的LLM上进行，验证了方法的跨架构泛化能力和可扩展性。
*   **数据集覆盖**：实验在三个不同类型的对齐基准数据集上进行。
*   **隐私预算测试**：实验测试了多种隐私预算（如`ε=0.5`， `1`， `1.5`， `2`），并考察了训练过程中的动态轨迹，提供了详细的隐私-效用权衡曲线。
*   **消融与鲁棒性研究**：通过与无偏标量扰动机制（Duchi， Piecewise）的对比，证明了PrivDPO设计的优越性；通过一个经验性的攻击（计算“记忆优势”），验证了其防御的真实有效性。实验设计全面、对比公平、结果充分。

### 6. 主要结论与发现

*   PrivDPO在提供正式的偏好隐私保证下，能有效对齐生产级LLM。
*   与DP-SGD、随机化响应等基线方法相比，PrivDPO在所有基准测试、模型和隐私预算下都**一致且显著地取得了更优的性能**，在`ε=1`时性能已非常接近非隐私的标准DPO。
*   该方法具有良好的可扩展性，可以成功应用于高达32B参数的模型上，且几乎没有增加额外的训练时间。
*   PrivDPO的梯度是一个无偏估计，其产生的误差远小于在高维空间加噪的DP-SGD。

### 7. 优点

*   **结构感知的隐私保护**：该方法巧妙地利用了DPO损失函数的特定几何结构，实现了精确到位的保护，避免了不必要的噪声注入，这是相比DP-SGD的主要进步。
*   **算法设计的简洁与高效**：PrivDPO将在高维梯度空间加噪的复杂问题，转化为对一个标量损失权重进行随机重采样的简单操作。这种设计使其可以作为标准DPO的即插即用替代方案，完美兼容现有的大规模训练框架，无需昂贵的逐样本梯度操作。
*   **理论保证与实践效果的统一**：方法不仅给出了严格的差分隐私式证明（ε-偏好隐私）和无偏性、误差界等理论分析，还在广泛的实验上验证了其优秀的实际效用。

### 8. 不足与局限

*   **威胁模型假设**：偏好隐私的有效性依赖于“提示和回复是非敏感的”这一假设。如果应用场景中提示或回复本身包含敏感信息（如个人病历），则此方法提供的隐私保护是不充分的，仍需依赖更强的传统差分隐私方法。该方法的成功高度依赖于这一假设的成立。
*   **泛化性局限**：该方法论的推导紧密依赖于标准DPO损失函数的数学形式，其隐私分析不能直接扩展到DPO的其他变体上（如KTO， IPO， SimPO等）。
*   **攻击评估有限**：虽然进行了一个经验上的简单攻击作为验证，但未考虑更强大、更多样的隐私攻击（如更复杂的成员推理攻击），对抗鲁棒性的证明可能不够充分。
*   **实验种子固定**：实验主要在固定随机种子下进行，虽然作者声称在前置实验中波动很小，但缺乏在不同随机种子下的多次平均结果，可能对结果的稳健性评估稍有影响。

（完）
