---
title: Secure Decentralized Federated Learning via Gossip and Virtual Voting
title_zh: 基于流言与虚拟投票的安全去中心化联邦学习
authors: "Amirhossein Taherpour, Xiaodong Wang"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08651v1"
tags: ["query:fl"]
score: 9.0
evidence: 使用流言传播和虚拟投票的具有拜占庭容错能力的去中心化联邦学习框架
tldr: 去中心化联邦学习（DFL）依赖gossip协议消除中央服务器，但缺乏模型来源的终结性，易受拜占庭和懒惰节点攻击。gspDAG-FL通过将gossip历史转化为拓扑DAG，运行Hashgraph虚拟投票，实现无全局协调的模型来源最终确认，并整合负载验证、接受证明验证和私有语义审计以增强鲁棒性。在MNIST和Penn Treebank上的实验表明，gspDAG-FL在N=100网络中的学习质量接近基于验证的账本FL，同时减少协调瓶颈，提升吞吐量，并有效检测无效模型来源，验证了其在对抗环境下的实用性和安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08651v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 351, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08651v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 1114, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08651v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1781, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08651v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1745, \"height\": 380, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08651v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1886, \"height\": 571, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1184, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 734, \"height\": 153, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1521, \"height\": 392, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1083, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 798, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08651v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1212, \"height\": 758, \"label\": \"Table\"}]"
motivation: 现有gossip DFL缺乏来源终结性与拜占庭鲁棒性，账本辅助FL则牺牲局部性引入全局协调。
method: gspDAG-FL利用gossip历史构建拓扑DAG，通过Hashgraph虚拟投票达成模型来源最终确认，并融合负载、接受证明与私有审计三层验证增强鲁棒性。
result: 在MNIST和Penn Treebank上，N=100网络中gspDAG-FL学习质量接近验证账本FL，同时降低协调开销、提升吞吐量，且无效源检测率高。
conclusion: gspDAG-FL为完全去中心化联邦学习提供了高效安全终结方案，有效抵御拜占庭与懒惰节点，提升系统鲁棒性。
---

## 摘要
去中心化联邦学习（DFL）通过让节点以点对点流言的方式交换模型更新来移除中心服务器，但现有基于流言的方法往往缺乏来源最终性以及对拜占庭或懒惰参与者的弹性。账本辅助的联邦学习（FL）提高了可审计性，然而区块链、分片或结算委员会可能会重新引入与DFL局部性相冲突的全局协调成本。本文提出gspDAG-FL，一个安全的DFL框架，它从用于传播模型的同一流言历史中导出共识。节点仅与邻居交换模型负载，而全节点收集事件证书和接收方背书的已接受流言证明，重建一个紧凑的拓扑有向无环图（DAG），并运行Hashgraph式虚拟投票，随后生成紧凑的全节点证书。最终性基于唯一的模型来源元组，而非相同的局部参数状态。为了提高弹性，gspDAG-FL在聚合前结合了负载验证、已接受证明验证和私有语义审计。我们形式化了对抗性设置，证明了控制平面的安全性和条件活性，并给出了在时变有效混合条件下认证扰动流言的收敛保证。在MNIST分类和Penn Treebank语言建模上的实验，使用公平的留出验证/审计数据以及多达N=100的网络，表明gspDAG-FL实现了接近基于验证的账本FL的学习质量，同时减少了协调瓶颈，提高了吞吐量，并在混合拜占庭和懒惰参与情况下保持了较高的无效来源检测能力。

## Abstract
Decentralized federated learning (DFL) removes the central server by letting nodes exchange model updates through peer-to-peer gossip, but existing gossip-based methods often lack provenance finality and resilience to Byzantine or lazy participants. Ledger-assisted federated learning (FL) improves auditability, yet blockchains, shards, or settlement committees can reintroduce global coordination costs that conflict with DFL locality. This paper proposes \emph{gspDAG-FL}, a secure DFL framework that derives consensus from the same gossip history used to disseminate models. Nodes exchange model payloads only with neighbors, while full nodes collect event certificates and receiver-endorsed accepted gossip proofs, reconstruct a compact Topology directed acyclic graph (DAG), and run Hashgraph-style virtual voting followed by compact full-node certificates. Finality is over unique model-origin tuples, not identical local parameter states. To improve resilience, gspDAG-FL combines payload validation, accepted-proof validation, and private semantic audit before aggregation. We formalize the adversarial setting, prove safety and conditional liveness of the control plane, and give a convergence guarantee for certified perturbed gossip under time-varying effective mixing. Experiments on MNIST classification and Penn Treebank language modeling, using fair held-out validation/audit data and networks up to \(N=100\), show that gspDAG-FL achieves learning quality close to validation-based ledger FL while reducing coordination bottlenecks, improving throughput, and maintaining high invalid-origin detection under mixed Byzantine and lazy participation.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有**去中心化联邦学习（DFL）** 通常依赖基于点对点流言（gossip）的模型交换，虽然消除了中心服务器，但缺少对模型更新**来源最终性（provenance finality）** 的定义与共识，导致其难以抵御拜占庭（Byzantine）节点和懒惰（lazy）节点的行为异常。
*   **现有方案困境**：引入区块链、DAG或委员会等账本机制虽能提升可审计性，却会重新引入**全局协调成本**（如区块确认、分片同步），与DFL追求的局部通信、水平扩展的初衷相冲突。
*   **核心目标**：提出一种**流言原生的共识层**，即直接从流言历史本身推导出哪些模型来源是可信且可被聚合的（最终性），而无需外挂独立的全局账本或委员会，从而实现安全、高效且完全去中心化的联邦学习。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 2.1 总体架构思想
*   **数据面与控制面分离**：模型参数仅经邻居节点流言传输（数据面，局部），而负责元数据（事件证书、接受证明）的**全节点（full node）** 通过收集流言历史构建拓扑DAG，并运行**Hashgraph式虚拟投票**来决定来源最终性（控制面，全局共识）。
*   **最终性对象**：共识的对象是**唯一的模型来源元组（origin tuple）** `(i, t, event_hash, model_hash)`，而非要求所有节点持有完全相同的模型参数状态，保证了局部性。

### 2.2 关键技术细节与流程
1.  **事件与拓扑DAG构建**：
    *   每轮通信（ripple），各节点（轻节点）发送**事件证书**（含自身父哈希、模型来源引用、签名）及可选的**接受证明**（发送证明+接收方背书）。
    *   全节点验证这些消息后，构建**拓扑DAG**：节点为事件，边为自父边或经接受的跨节点流言父边，形成有向无环图。
    *   **空事件（heartbeat）** 用于维持拓扑连续性，但无投票权。
2.  **虚拟投票与局部确认**：
    *   全节点基于DAG的祖先可达性递归计算每个事件的**时刻（moment）** 和**可达合格事件集**。
    *   当某事件的可达集规模≥ `⌊2N/3⌋+1` 时，该事件成为**投票事件**，其投票基于子集多数；进一步当某投票事件的子集票数满足超级多数时，形成**中间确认**。该过程即为**虚拟投票**。
    *   若某全节点本地发现存在投票事件确认了某个来源元组，即产生**局部确认**。
3.  **证书交换与全局终结性**：
    *   全节点间交换局部确认向量；当某个来源元组获得至少 `⌊2N_F/3⌋+1` 个全节点的局部确认时，该元组进入**全局认证集** `C(t)`。
    *   当足够多的控制面正确节点（≥ τ_N）观察到足够数量（Q）的已认证来源时，全节点广播**终结证书**，epoch结束。
4.  **三级验证管线以增强鲁棒性**：
    *   **负载验证（Payload Validation）**：检查更新是否陈旧，数值幅度是否异常（基于历史均值±3σ），梯度方向是否与历史平均方向一致（余弦相似度筛选）。
    *   **接受证明验证（Accepted‑proof Validation）**：验证发送方签名、接收方背书、自父哈希、来源哈希等，防止伪造边、重复事件或equivocation。
    *   **私有语义审计（Private Semantic Audit）**：在共识完成后，每个节点利用本地私有触发集，检测模型输出向量相对于上一轮的变化，使用中位数+3倍MAD规则去除语义异常模型，而后进行加权聚合。

### 2.3 理论保证（简要）
*   证明了在存在少于1/3拜占庭全节点的条件下，控制平面的**安全性**（不会有两份不同终结证书）、虚拟投票**一致性**，以及**条件活性**（若足够多合格来源被扩散，则终将终止）。
*   给出了在**时变有效混合矩阵**和追踪误差条件下的**收敛性定理**，表明认证后的扰动流言聚合可收敛到学习‑正确目标的稳定邻域。

## 3. 实验设计：数据集、场景、对比方法
### 3.1 数据集与任务
*   **任务1**：MNIST图像分类，使用轻量CNN（基于MobileNetV2）；语义攻击为3×3白方格触发器，目标类为0。
*   **任务2**：Penn Treebank语言建模，使用GRU预测器；语义攻击为固定双词触发器，预测目标token。
*   **数据划分**：所有方法使用相同训练预算；公共验证集（用于BLADE‑FL, ChainFL）与私有审计集（用于gspDAG‑FL）均从留出样本中抽取，测试集独立。

### 3.2 对比基准
*   **AD‑PSGD**：纯去中心化流言SGD，无最终性层，无验证。
*   **BLADE‑FL**：基于区块链工作量证明的FL，更新通过交易全局传播，矿工用公共验证集筛选。
*   **ChainFL**：基于分片DAG账本的FL，分片内共识与主链提交。
*   **gspDAG‑FL**：本文方法。

### 3.3 网络与故障配置
*   **默认N**=15，目标拜占庭比例μ=0.15，懒惰比例γ=0.1，全节点比0.40，准备阈值 Q=⌊2N/3⌋+1，Rmax=Q+2。
*   **网络大小扫描**：N=5,10,15,20,25,30,50,75,100；μ、γ也进行了单独扫描。
*   **拓扑**：gspDAG‑FL和AD‑PSGD使用Watts–Strogatz小世界图；BLADE‑FL与ChainFL使用其原生的全连接或分片结构。
*   **硬件参数**：节点带宽10‑50 Mbps，延迟50‑200 ms；本地SGD配置 B=50, K=5, η=0.01, λ=100。

## 4. 资源与算力
*   文中**未明确说明**具体的GPU型号、数量或训练总时长。
*   技术栈提及：控制面仿真使用Kotlin/JVM、JGraphT、Ed25519（BouncyCastle）；学习部分使用Python 3.10+PyTorch；通信使用gRPC；编排用Docker Compose。实验规模较小（最大N=100），属于仿真环境，未涉及大规模GPU集群的训练算力报告。

## 5. 实验数量与充分性
*   **实验内容**：
    1.  不同N下的学习性能（准确率/困惑度）。
    2.  不同μ、γ下的鲁棒性扫描。
    3.  防御管线各阶段的检测数量及误报率分析。
    4.  Ripple动态随epoch变化的统计。
    5.  系统扩展性：标准化延迟、吞吐量、收敛轮次与节点数N的关系。
*   **充分性与公平性**：
    *   实验覆盖了两种不同类型任务（CV与NLP），多种网络规模和故障比例，较为全面。
    *   对比基线包含纯流言、区块链、分片DAG等多种范式，且所有方法均使用相同训练总预算，**公平性得到保证**。
    *   防御有效性通过检测率和误报率量化，并拆解了各个过滤阶段的贡献，**消融分析较为清晰**。
    *   系统效率比较采用标准化比值，排除了绝对硬件差异，突出**架构级扩展性**。

## 6. 论文的主要结论与发现
1.  **学习质量**：gspDAG‑FL在含拜占庭/懒惰节点的环境下，学习质量与验证依赖的账本FL（BLADE‑FL, ChainFL）接近，但明显优于无最终性层的纯流言法（AD‑PSGD）。
2.  **故障影响**：拜占庭节点比懒惰节点更具破坏性（改变方向/语义），但gspDAG‑FL的多级过滤有效抑制了两种影响。
3.  **防御有效性**：综合检测率高达96%左右，误报率低于0.4%；早期依赖幅度方向过滤，后期语义审计发挥作用。
4.  **系统扩展性**：gspDAG‑FL将模型张量保留在局部流言路径，仅将轻量来源证明送入控制面，因此在N增大时，**吞吐量扩展趋势明显优于BLADE‑FL和ChainFL**，延迟增长更平缓，收敛轮数稳定下降。
5.  **共识机制**：拓扑DAG+虚拟投票可在不引入单独账本的前提下，为去中心化FL提供安全的来源最终性。

## 7. 优点：方法或实验设计上的亮点
*   **流言原生的最终性方案**：巧妙利用流言历史本身推导共识，是首个真正将数据面与控制面解耦、且共识机制不破坏DFL局部性的工作。
*   **仅认证来源元组**：最终性不要求模型参数全局一致，在保留异步局部聚合的同时实现了可信溯源，设计精巧。
*   **多层纵深防御**：负载验证→证明验证→私有审计的管线设计，分别应对异常数值、拓扑伪造和语义后门，抗拜占庭能力层次分明。
*   **严格的理论支撑**：提供了控制面的安全性和活性证明，以及学习收敛保证，理论深度与系统设计紧密结合。
*   **全面的系统性评估**：实验既包含学习质量，又包含防御管线分解、ripple动态和系统吞吐/延迟扩展性，评价维度完整。

## 8. 不足与局限
*   **实验规模有限**：最大节点数仅为100，且未测试在真实边缘网络拓扑、高动态网络波动下的表现；未来工作也提及需研究动态节点变化（churn）。
*   **隐私保护薄弱**：虽然审计是私有的，但控制面传播的事件证书、接受证明等元数据本身可能泄露模型更新或节点关系信息，论文未涉及强隐私机制（如差分隐私或安全聚合）。
*   **全节点假设**：需依赖一组行为相对诚实的全节点（拜占庭比例<1/3），在完全无信任的开放场景中，全节点可靠性和激励机制未讨论。
*   **参数敏感性**：负载验证中的幅度、方向阈值依赖历史统计，若初期就存在大量拜占节点，可能导致正常更新被误拒；文中承认在参考模型被持续投毒时审计降级为经验性。
*   **通信开销**：虽然数据面是局部流言，控制面需每轮将事件证书和证明发送给全节点，全节点间还需交换确认向量，在N较大时元数据总量仍会增加，未与纯流言方法的控制开销做详细对比。
*   **收敛定理条件**：要求有效混合矩阵满足收缩性等强假设，实际网络动态可能使得条件难以始终成立。

（完）
