---
title: "FedOPAL: One-Shot Federated Learning via Analytic Visual Prompt Tuning"
title_zh: FedOPAL：通过解析视觉提示微调实现一次性联邦学习
authors: "Lingyu Qiu, Daniela Annunziata, Stefano Izzo, Fabio Giampaolo, Francesco Piccialli"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08368v1"
tags: ["query:fl"]
score: 9.0
evidence: 通过解析提示调优解决单轮联邦学习中非IID数据和通信效率问题。
tldr: 在边缘智能中，基础模型的广泛应用使通信带宽成为联邦学习的瓶颈。单轮联邦学习虽能减少通信轮次，但现有方法面临服务器计算成本高、超参数敏感以及非独立同分布下特征假设失效等问题。为此，FedOPAL框架提出以视觉提示为特征修正器，通过局部近端约束主动将异构数据特征校正至线性可分空间，满足解析联邦学习的理论条件。实验显示，该方法在多个基准上显著优于原有解析方法，精度可比肩迭代式前沿方法，且服务器端零训练成本。这为边缘大模型高效协作提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08368v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1873, \"height\": 838, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08368v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 928, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08368v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 828, \"height\": 705, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08368v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1334, \"height\": 466, \"label\": \"Table\"}]"
motivation: 解析联邦学习在非独立同分布数据下静态特征假设失效，导致特征流形错位，亟需有效的特征修正机制。
method: FedOPAL以视觉提示为特征修正器，通过局部近端约束主动将非IID特征校正到线性可分空间，确保解析联邦学习假设成立。
result: FedOPAL在多个基准上显著超越原有解析方法，精度达到迭代式前沿方法水平，且服务器端计算开销为零。
conclusion: FedOPAL通过视觉提示校正特征，突破了解析联邦学习在非IID数据下的瓶颈，为零服务器训练成本的边缘大模型协作提供了新范式。
---

## 摘要
随着基础模型在边缘智能中的广泛部署，通信带宽已成为限制联邦学习可扩展性的核心瓶颈。尽管一次性联邦学习通过最小化通信轮次缓解了这一问题，但现有的迭代微调或知识蒸馏方法仍面临服务器端计算成本高和超参数敏感等挑战。解析联邦学习利用最小二乘闭式解实现高效的无梯度聚合，但在非独立同分布数据环境中，其静态特征假设失效，导致特征流形错位并严重损害模型性能。为了解决这一矛盾，本文提出了FedOPAL框架。该框架采用视觉提示作为特征校正器，通过施加局部近端约束主动将异构数据的特征分布校正到线性可分空间，从而满足解析联邦学习的理论假设。实验结果表明，FedOPAL不仅在多个基准测试上显著优于原有解析方法，而且在与最先进的迭代方法相当的精度的同时保持零服务器端训练成本，为边缘上大模型的高效协作提供了一种新的工程范式。

## Abstract
With the widespread deployment of basic models in edge intelligence, communication bandwidth has become a core bottleneck restricting the scalability of federated learning. Although one-shot federated learning alleviates this problem by minimizing communication rounds, existing iterative fine-tuning or knowledge distillation methods still face challenges such as high server-side computational costs and hyperparameter sensitivity. Analytical federated learning achieves efficient gradientfree aggregation using least-squares closed-form solutions, but in environments with non-independent and identically distributed data, its static feature assumptions fail, leading to feature manifold misalignment and severely impairing model performance. To address this contradiction, this paper proposes the FedOPAL framework. This framework adapts the visual prompts as feature rectifiers, actively correcting the feature distribution of heterogeneous data to a linearly separable space by applying local proximal constraints, thereby satisfying the theoretical assumptions of analytical federated learning. Experimental results show that FedOPAL not only significantly outperforms the original analytical methods on several benchmarks, but also achieves accuracy comparable to state-of-the-art iterative methods while maintaining zero server-side training costs, providing a new engineering paradigm for efficient collaboration of large models on the edge.

---

## 论文详细总结（自动生成）

## 1. 核心问题与研究动机  
- **通信瓶颈**：基础模型（如 ViT、CLIP）参数量激增，传统联邦学习的高频参数交互在带宽受限的边缘场景（自动驾驶、卫星通信）中难以部署。  
- **一次性联邦学习的困境**：单轮通信虽减少交互，但现有方法（知识蒸馏、数据合成）将计算压力转移至服务器，且超参数敏感；解析联邦学习（AFL）通过最小二乘闭式解实现无梯度聚合，效率极高，却要求“冻结骨干提取的特征在异构客户端间保持线性可分”，这一假设在非独立同分布（Non‑IID）数据下崩溃，导致特征流形错位、性能骤降。  
- **本文意图**：找到一个能在保持零服务器训练成本与单轮通信的前提下，主动校正非 IID 特征分布以满足解析学习前提的方法。  

## 2. 方法论  
### 2.1 核心思想  
将**视觉提示（Visual Prompt）** 视为**特征分布调制器**：在冻结的 ViT 嵌入序列中插入少量可学习的连续 token，通过局部优化引导骨干将异构数据映射到线性可分空间，随后利用解析学习直接求得最优全局分类器。  

### 2.2 关键技术细节  
- **基础组件**：  
  - 解析联邦学习：对 \(K\) 个客户端，全局线性分类器 \(W^*\) 的闭式解为 \(W^* = (\sum_{k}R_k + \lambda I)^{-1}(\sum_{k}C_k)\)，其中 \(R_k\)、\(C_k\) 为局部特征的自相关与互相关矩阵。  
  - 视觉提示调优（VPT‑Shallow）：在类别 token 与 patch 嵌入之间插入 \(M=10\) 个可学习提示 token \(P\in\mathbb{R}^{M\times D}\)，仅优化 \(P\)，骨干完全冻结。  
- **FedOPAL 流程**：  
  1. **本地阶段**（各客户端独立执行）：  
     - 初始化本地提示 \(P_k\) 与临时线性头 \(W_k\)。  
     - 通过梯度下降优化目标：\(\min_{P_k,W_k} \mathcal{L}_{CE}(W_k^\top f_\theta(Z_k(x))[CLS], y) + \frac{\mu}{2}\|P_k - P_{init}\|^2\)，近端项 \(\mu\) 平衡本地适应与全局稳定。  
     - 用收敛后的 \(P_k\) 前向计算校正特征 \(h_i = f_\theta(Z_k(x_i))[CLS]\)，并提取本地充分统计量 \(R_k, C_k\)。  
  2. **服务器阶段**（单轮通信）：  
     - 提示平均：\(P_{global} = \frac{1}{K}\sum_k P_k\)。  
     - 解析聚合：利用所有 \(R_k, C_k\) 按上述闭式解直接算出全局分类器 \(W^*_{global}\)。  
  3. **推理**：客户端下载 \(P_{global}\) 和 \(W^*_{global}\)，对测试样本同样插入全局提示后分类。  

## 3. 实验设计  
### 3.1 数据集与场景  
- **数据集**：CIFAR‑10、CIFAR‑100（粗/细粒度物体）、SVHN（街景门牌号）、DTD（纹理）。  
- **数据划分**：Dirichlet 分布 \(\alpha \in \{0.01,0.1,0.5,1.0,10\}\)，模拟从极度 Non‑IID 到 IID；默认客户端数 \(K=10\)，\(\alpha=0.1\)。  

### 3.2 对比方法  
- **KD/生成器类**：Co‑Boosting、DENSE。  
- **预训练统计类**：AFL、FedPFT、FedCGS。  
- **主干网络**：FedOPAL 使用 CLIP ViT‑B/16，基线方法沿用各自原文设定（多为 ResNet‑18/50），以展示基础模型先验带来的优势。  

### 3.3 评估指标  
- 各数据集上的分类准确率。  

## 4. 资源与算力  
- **硬件**：所有实验均在 **1 块 NVIDIA A6000 GPU** 上完成。  
- **时长**：论文未明确报告训练时间。  
- **框架**：PyTorch。  

## 5. 实验数量与充分性  
- 共涉及 **4 个数据集 × 5 种异质程度（\(\alpha\)）**，生成了丰富的性能对比表（Table I、II）。  
- 消融实验分析了 **近端系数 \(\mu\)、解析正则化 \(\lambda\)、本地训练轮数 \(E\)** 对性能的影响（Table III），覆盖两种 \(\alpha\) 取值。  
- 对比方法涵盖两类 SOTA，并且列出了 Non‑IID 与 IID 条件下的结果，评估较为全面。  
- **公平性**：基线方法未统一使用相同骨干网络，可能部分放大 FedOPAL 的效果，但作者在文中已说明各自采用的原设定。  

## 6. 主要结论与发现  
- FedOPAL 在 Non‑IID 场景下大幅超越原有解析方法（CIFAR‑100 \(\alpha=0.01\) 时领先约 10%），并与迭代式 SOTA 方法精度相当，且**服务器端零训练成本**。  
- 视觉提示能有效校正特征流形，使解析解在极端异质条件下仍稳定；模型性能对不同 \(\alpha\) 几乎不变（CIFAR‑10 波动仅 0.32%），成功“解耦模型性能与客户端数据分布”。  
- 超参数 \(\lambda\) 影响微小（<0.5%），表明特征矩阵天然良态，无需繁琐调参。  

## 7. 优点  
- **高效架构**：单轮通信 + 服务器端纯代数运算，极大降低系统开销。  
- **非 IID 鲁棒性**：利用视觉提示显式对齐特征空间，解决了统计联邦学习的关键失效点。  
- **实验扎实**：多数据集、多异质程度、全面消融，验证了方法的有效性与稳定性。  
- **工程友好**：超参数不敏感，利于实际部署。  

## 8. 不足与局限  
- **领域泛化**：SVHN 数据集上性能弱于 FedCGS，因 CLIP 预训练主要面向自然图像，数字识别域差异较大；对于其他特殊域（如医学影像）可能同样需要额外适应。  
- **本地计算**：虽然服务器零训练，但客户端仍需梯度优化视觉提示，未完全消除本地训练成本。  
- **公平性对比**：基线方法未统一使用强大骨干，结果优势部分来源于 CLIP 的先验能力，而非纯粹的方法改进。  
- **缺失分析**：未讨论上传提示和统计矩阵的通信量、隐私保护机制，且仅测试图像分类任务。  

（完）
