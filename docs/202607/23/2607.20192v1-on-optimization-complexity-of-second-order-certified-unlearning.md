---
title: On Optimization Complexity of Second-Order Certified Unlearning
title_zh: 关于二阶认证遗忘的优化复杂性
authors: "Nikita Doikov, Anastasia Koloskova"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20192v1"
tags: ["query:machine-unlearning"]
score: 9.0
evidence: 提出带各向异性高斯噪声的二阶认证遗忘算法及优化复杂性分析
tldr: 针对机器遗忘问题，本文从优化视角研究认证遗忘的算法复杂度，目标兼顾认证遗忘与优化准确性。利用一致凸正则化与泛化误差替代，建立遗忘前后模型距离新界，证明数据可预测性降低优化难度。提出一种结合各向异性高斯机制的二阶遗忘算法，在拟自和谐损失下实现快速认证遗忘。理论应用于逻辑回归等模型，验证了二阶方法相比一阶方法的可证明优势。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20192v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 786, \"height\": 284, \"label\": \"Table\"}]"
motivation: 探究认证遗忘的优化复杂度，旨在同时保障数据移除的认证性与模型准确性。
method: 设计各向异性高斯噪声注入的二阶遗忘算法，并基于一致凸正则化推导遗忘前后距离上界。
result: 给出遗忘前后模型距离的理论界，揭示数据可预测性令优化简化；二阶算法在拟自和谐损失下实现快速收敛，并优于一阶方法。
conclusion: 为认证遗忘建立了优化复杂度理论，证明二阶方法在计算上优于一阶方法，对线性模型遗忘具有理论与实用价值。
---

## 摘要
我们研究机器遗忘：从训练好的模型中移除所记忆的训练数据。特别地，我们从优化视角研究认证遗忘的算法复杂性。我们将遗忘算法的目标形式化为同时实现认证遗忘与优化精度。利用一致凸正则化项的概念，我们通过新颖的泛化误差替代量，证明了初始模型与遗忘后模型之间距离的新界限。由此，我们在理论上证明，若被移除的数据能被遗忘后模型良好预测，则相应的优化问题是简单的。此外，我们发展了一种新的二阶遗忘算法，该算法采用各向异性高斯机制并具有最先进的全局收敛性。我们证明，对于具有拟自和谐损失的线性模型，该方法在实现认证遗忘方面具备快速收敛速率。作为直接应用，我们的理论涵盖了逻辑回归和指数回归中的遗忘，并展示了相比于仅使用一阶信息的遗忘方法，利用二阶信息具有可证明的优势。

## Abstract
We study machine unlearning: the removal of memorized training data from a trained model. Specifically, we investigate the algorithmic complexity of certified unlearning from an optimization perspective. We formalize the goal of an unlearning algorithm as simultaneously achieving certified unlearning and optimization accuracy. Utilizing the notion of uniformly convex regularizers, we prove new bounds on the distance between initial and unlearned models using a novel substitute for generalization error. Thus we theoretically demonstrate that if the removed data is well-predicted by the unlearned model, the corresponding optimization problem is simple. Furthermore, we develop a new second-order unlearning algorithm with an anisotropic Gaussian mechanism and state-of-the-art global convergence. We prove fast rates for our method in achieving certified unlearning for linear models with quasi-self-concordant losses. As a direct application, our theory covers unlearning for logistic and exponential regressions and shows a provable benefit of utilizing second-order information compared to first-order unlearning methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
本文聚焦**机器遗忘**问题：如何从已训练模型中高效删除特定训练数据的影响，使得删除后的模型如同从未学习过这些数据（即 **认证遗忘**），同时保持对保留数据的优化精度。作者从优化理论角度研究认证遗忘的算法复杂度，目标是在给定三元组（隐私参数 `q, δ` 和优化容忍度 `ϵ`）下，同时实现两个保证：
- **认证遗忘保证**：删除数据后模型的输出分布与从保留数据从头训练的模型分布不可区分（`(q,δ)`-不可区分性）。
- **优化精度保证**：遗忘后模型与保留数据精确最优解的距离不超过 `ϵ`。

其整体含义是：若被删除的数据可被遗忘后模型很好地预测（即泛化误差小），则相应的优化问题是简单的；并且通过**利用二阶信息**设计的算法，能在一阶方法的基础上获得可证明的加速。

## 2. 论文提出的方法论
### 2.1 核心思想与公式
将训练目标分解为 `F(x) = (1-γ)f(x) + γu(x)`，其中 `f` 为保留数据损失，`u` 为遗忘数据损失，`γ` 代表遗忘数据比例（通常很小）。定义**预测误差量**：
`π(x) := ∥∇u(x) + ψ′(x)∥∗` （其中 `ψ` 为一致凸正则化项）。
在**一致凸正则化**（如 `ℓ₂` 正则化，凸性次数 `p≥2`）前提下，作者推导出关键引理（Lemma 2.1）：
- 初始模型 `x⋆F` 与遗忘后最优解 `x⋆f` 之间的距离受 `(γ/μ)·π(x⋆f)` 控制（`μ` 为凸性常数）。
- 函数残差受 `[γ·π(x⋆F)/(1-γ)]` 控制。  
这表明：若遗忘数据被遗忘后模型预测得好（`π(x⋆f)` 小），或遗忘比例 `γ` 小、正则化强度 `μ` 大，则遗忘任务在优化上更简单。

### 2.2 算法流程
提出 **二阶认证遗忘算法**（Algorithm 1），核心步骤：
1. 初始化 `x0 = x⋆F`（全数据最优解）。
2. 进行 `K` 次迭代，每次最小化二阶模型：
   `xₖ₊₁ = arg minₓ {⟨∇f(xₖ), x⟩ + ½⟨Hₖ(x−xₖ), x−xₖ⟩ + ψ(x)}`
   其中 `Hₖ` 包含 Hessian 信息。在本文最强的二阶实例中，采用 **梯度正则化的牛顿法**：
   `Hₖ = ∇²f(xₖ) + M gₖ B`，`gₖ = ∥∇f(xₖ)+ψ′(xₖ)∥∗`，`B` 为保留数据协方差矩阵（可定义全局范数）。
3. 在输出前，注入**各向异性高斯噪声**：`ξ ∼ N(0, σ²B⁻¹)`，噪声协方差与 `B⁻¹`（即二阶几何）对齐。
4. 输出 `yₖ = xₖ + ξ`。

### 2.3 认证遗忘与优化的绑定
通过噪声标准差 `σ` 的设定（依赖于优化距离 `ε` 和隐私参数 `q,δ`），可在满足 `(q,δ)`-认证遗忘的同时，保证 `E[∥yₖ−x⋆f∥] ≤ √(ε²+σ²d)`。定理 3.3 给出了 `σ` 的显式条件。  
最终复杂性（Corollary 3.4）：牛顿法仅需 **对数级**迭代（在 `ϵ` 上），远优于一阶梯度法的多项式依赖。

## 3. 实验设计
**本文是纯理论论文，未包含任何实验或数值模拟**。文中仅通过**理论实例**（线性模型、逻辑回归、指数回归）验证假设的合理性：
- 线性模型：损失为 `ℓ(⟨aᵢ, x⟩)`，遗忘部分对应 `γ = m/N`。
- 拟自和谐损失（包含平方损失、指数损失、逻辑损失）满足二阶光滑性假设。
- 正则化器 `ψ` 可覆盖 `ℓ₂` 正则化、弹性网、甚至凸约束指标函数。
论文未使用任何数据集、benchmark 或对比方法，也没有实验表格或图表。所提供的表格截图实际是算法框架的伪代码描述，并非实验结果。

## 4. 资源与算力
**文中未提及任何计算资源或算力消耗**，因未进行实际实验，自然不存在 GPU 型号、数量、训练时长等描述。

## 5. 实验数量与充分性
**无实验**，故实验数量为零。论文的“验证”完全依靠理论证明，包括：
- 对梯度法和牛顿法的收敛速率推导（定理 3.1, 3.2）。
- 对认证遗忘和优化精度的联合保证（定理 3.3）。
- 所有假设均在经典线性模型和常见损失上成立，逻辑自洽。
从纯理论工作的角度，证明与推导是充分的，但其工程实效性缺乏实验支撑。

## 6. 论文的主要结论与发现
- **距离–预测误差联系**：遗忘后最优解与原始最优解的距离受限于遗忘数据在遗忘后模型上的预测误差，并且该距离可通过增大正则化系数或减小遗忘比例来压缩。
- **二阶算法的优越性**：对于拟自和谐损失的线性模型，带有梯度正则化的牛顿法达到全局快速收敛，复杂度中优化精度 `ϵ` 仅出现在对数项，而一阶梯度法的复杂度正比于 `ε^{-(p-2)/p}`（当 `p>2` 时呈多项式依赖）。
- **各向异性噪声机制**：协方差与 Hessian 对齐的高斯噪声不仅满足认证遗忘，还自然适配目标函数的局部几何，优化意义明确。
- **理论与应用覆盖**：理论覆盖了逻辑回归、指数回归等常见模型，为这些模型下的认证遗忘提供了可证明的效率保证。

## 7. 优点
- **首次为二阶方法在认证遗忘中建立全局复杂度**，突破以往仅限一阶或局部收敛的分析。
- **将优化难度与数据可预测性挂钩**，为理解遗忘问题的本质提供新视角。
- **各向异性噪声设计**巧妙利用模型二阶信息，使得认证噪声的方差与数据几何自适应匹配，可能在实际中更高效。
- **统一的凸正则化框架**（一致凸性）增强了分析的普适性，不局限于强凸。
- 理论结果对遗忘数据比例 `γ` 的依赖清晰，符合“少量数据遗忘”的典型场景。

## 8. 不足与局限
- **完全缺乏实验验证**，对于实际数据上的表现、算力需求、超参数敏感性等无法评估。
- **假设严格**：要求初始模型为全数据精确最优解（`x⋆F`），且损失函数满足拟自和谐条件，这些在实际（非凸）深度学习模型中难以保证。
- **仅限于线性模型**（广义线性模型），未涉及深度神经网络或其他非线性结构。
- 牛顿法每一步需要计算 Hessian 并求解子问题，计算代价高昂（文中提到可通过一阶子程序求解，但未讨论实际计算成本）。
- 认证遗忘的 `σ` 设定依赖于优化距离 `ε`，而 `ε` 可能难以事先精确获得（需要先验或自适应策略）。
- 一阶基线分析中假设了损失二阶导数的全局上界（Lipschitz梯度），这可能在某些模型中过于保守。
- 论文未讨论当遗忘请求多次连续到来时（持续遗忘）的累积效应。

（完）
