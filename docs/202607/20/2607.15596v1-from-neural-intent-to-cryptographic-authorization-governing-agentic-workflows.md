---
title: "From Neural Intent to Cryptographic Authorization: Governing Agentic Workflows"
title_zh: 从神经意图到密码学授权：治理智能体工作流
authors: "Jiasi Weng, Jian Weng, Minrong Chen, Ming Li, Jia-Nan Liu, Zhi Li, Yue Zhang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15596v1"
tags: ["query:agent-security"]
score: 9.0
evidence: 通过神经符号密码授权防止LLM代理被提示注入劫持
tldr: AI代理工作流日益普及，传统密钥管理仅认证调用者身份，无法防止提示注入导致代理执行越权操作，构成重大安全威胁。为此，本文提出神经加密服务NCS，构建神经-符号主动安全治理层，将不可信LLM规划与加密强制执行解耦，利用离线签名与哈希链确保每条指令按序释放并强制绑定工具参数。在AgentDojo和参数劫持基准上，NCS将攻击成功率降至近零，且不影响良性任务执行。NCS将代理安全核心从检测意图合规转向验证操作与加密授权的一致性，显著提升了自治工作流的安全治理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15596v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1478, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15596v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 936, \"height\": 597, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15596v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 896, \"height\": 764, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15596v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1465, \"height\": 1123, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15596v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1768, \"height\": 949, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15596v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 820, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15596v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 898, \"height\": 336, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15596v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 879, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15596v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1309, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15596v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1678, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15596v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1738, \"height\": 1030, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15596v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 948, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15596v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 606, \"height\": 242, \"label\": \"Table\"}]"
motivation: 现有密钥服务仅认证调用者，无法阻止提示注入劫持合法代理执行越权操作，亟需工作流级安全治理。
method: 提出神经加密服务NCS，采用神经-符号架构解耦规划与执行，通过离线签名哈希链逐指令释放并强制参数绑定，实现加密门控。
result: 实验显示，NCS在AgentDojo及自定义参数劫持基准上将攻击成功率降至近零，且良性工作流效用损失微小。
conclusion: NCS通过加密授权绑定重构代理安全范式，将验证重心从意图合规转向操作与授权匹配，显著增强自治工作流安全性。
---

## 摘要
人工智能驱动和智能体工作流的快速采用，正将传统的政府与企业系统转变为基于语言、使用工具且日益自主的基础设施。传统密钥管理服务验证谁可以调用密码学原语，但在运行时对哪些工作流步骤被授权却毫不知情：一个已认证的代理仍可能被直接或间接的提示注入劫持，执行满足身份检查但违反用户意图的恶意操作。我们提出神经密码学服务（NCS），这是一个主动的安全治理层面，基于神经-符号设计并置于大语言模型代理与特权工具之间。在NCS下，一个不可信的神经规划器将自然语言指令编译为结构化的计划草案，但并不持有执行权限。执行由确定性符号控制器把关，该控制器操作的是离线签名、哈希链化的指令流。NCS验证签名，增量地校验哈希链，每次精确释放一个指令载荷，并强制代理提议的工具参数与已验证载荷之间的严格绑定。不匹配或乱序的工具调用会被安全拒绝，同时先前已验证的状态被保留以供事后审计。我们在AgentDojo和一个自定义的参数劫持基准上评估NCS。NCS将攻击成功率降至接近零，同时在良性工作流上保持可接受的效用。NCS因此将代理安全重新定义为：从询问模型的意图是否合规，转变为询问所提议的调度是否与密码学授权的步骤相匹配。

## Abstract
The rapid adoption of artificial intelligence (AI)-driven and agentic workflows is transforming traditional government and enterprise systems into language-based, tool-using and increasingly autonomous infrastructures. Conventional key management services authenticate who may invoke a cryptographic primitive, but remain agnostic to which workflow steps are authorized at runtime: an authenticated agent can still be hijacked by direct or indirect prompt injection into executing malicious actions that satisfy identity checks yet violate user intent. We present \emph{Neural Cryptographic Services} (NCS), an active security governance plane grounded in a neural-symbolic design and interposed between LLM agents and privileged tools. Under NCS, an untrusted neural planner compiles natural-language instructions into structured plan drafts but holds no execution authority. Execution is instead gated by a deterministic symbolic controller operating over an offline-signed, hash-chained instruction stream. NCS verifies the signature, incrementally validates the hash chain, releases exactly one instruction payload at a time, and enforces strict binding between the agent's proposed tool parameters and the verified payload. Mismatched or out-of-order tool calls are rejected fail-closed, while previously verified states are retained for post-hoc auditing. We evaluate NCS on AgentDojo and a custom argument-hijacking benchmark. NCS drives attack success rates to near zero while preserving acceptable utility on benign workflows. NCS thus reframes agent security from asking whether a model's intent complied to asking whether a proposed dispatch matches a cryptographically authorized step.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型驱动智能体工作流在政府和金融等关键领域的快速部署，传统的密码学服务（如硬件安全模块HSM、密钥管理服务KMS）暴露出根本性的安全缺口：它们仅能被动验证“**谁**（who）在调用”，却完全不感知“**为什么**（why）”、“**何时**（when）”以及“**在何种工作流条件下**（under what workflow conditions）”应批准该操作。这导致一个已认证的合法代理可能被直接或间接提示注入攻击劫持，执行满足身份检查但完全违背用户意图的恶意动作（例如向攻击者发送敏感邮件），而底层密码服务却毫无阻拦能力。  
论文旨在将密码服务从被动的执行引擎**升级为主动的运行时安全治理平面**，通过密码学手段对智能体工作流中的**指令真实性**和**运行时控制流完整性**进行强制性约束，解决意图到操作之间的授权断裂问题。

### 2. 论文提出的方法论
#### **核心思想：神经–符号架构（Neural‑Symbolic Design）**
NCS（Neural Cryptographic Services）严格分离“概率性的神经意图解释”与“确定性的符号权限执行”：
- **神经侧（神经规划器 Neural Planner）**：负责将自然语言指令编译为结构化的计划草案（JSON模式），但该草案**不具备任何执行权限**。
- **符号侧（符号控制器 Symbolic Controller + 密码执行引擎 + 验证器）**：对离线签名、哈希链连接的指令流进行确定性验证，唯有通过验证的指令才会被逐步释放并强制绑定到代理的工具调用参数上。

#### **关键技术：有状态指令认证（Stateful Instruction Authentication）**
采用**流签名（Stream Signature）** 与**哈希链**相结合的方案，实现“1 + N”高效验证，分两步进行：
1. **构造阶段（用户侧）**：  
   - 将多步指令块 $B_1, \dots, B_k$ 通过反向哈希链构建：$B'_i = \text{enc}(B_i \parallel h_{i+1})$，其中$h_{i+1}=H(B'_{i+1})$，末尾用零向量锚定防止追加。  
   - 仅对流头元数据 $M_0 = h_1 \parallel k$ 进行非对称签名 $\sigma$，形成首块 $B'_0$。
2. **验证与执行阶段（NCS侧）**：  
   - **Phase A（签名验证）**：验证 $B'_0$ 的签名以确认**工作表级别的授权**，成功则建立可信根。  
   - **Phase B（逐块执行与绑定）**：每收到一个链块 $B'_i$，先验证哈希连续性 $H(B'_i) \stackrel{?}{=} A_{i-1}$，通过后释放明文指令 $B_i$；代理提出的工具调用参数必须与 $B_i$ 严格等价匹配，否则后续操作被拒绝。  
   - 所有工具分派均须经由**验证器（Verifier）**的故障闭锁（fail‑closed）门控，未通过验证的操作完全无法执行。

通过这一设计，神经规划器即使被完全欺骗，也无法触发任何未经密码学签名的控制动作，将安全边界从“模型意图是否合规”转移到“工具调度是否与密码学授权步骤精确匹配”。

### 3. 实验设计
#### **数据集与场景**
- **AgentDojo**：作为主要基准，覆盖工单、旅行、银行三个领域，评估对间接提示注入（IPI）的防御能力。
- **InjecAgent-ArgHijack**：从InjecAgent衍生，专门用于测试**工具参数劫持**攻击，包含85个案例（5种金融控制工具×17种读取工具），如修改收款人、金额等。
- **OpenPromptInjection**：用于构造针对FATH防御的绕过攻击（QA和URL注入任务）。

#### **对比方法**
- **基线（Baseline）**：无防御。
- **Spotlighting**：输入变换，通过标记将数据与指令分离。
- **FATH**：基于文本鉴真，要求LLM主动将内容分类为授权/未授权标签。
- **FIDES**：基于信息流控制（IFC）的防御。
- **NCS自身的消融配置**：NCS‑A（仅签名验证）、NCS‑B（签名+哈希链）、NCS‑Full（签名+哈希链+参数强制绑定）。

#### **评估模型**
DeepSeek-Chat, GPT‑4o‑mini, GPT‑5 Reasoning, GPT‑5‑Chat。

#### **评估指标**
- **ASR_tool**：攻击者成功触发未授权工具执行的比例。
- **ASR_args**：参数被劫持但工具名正确的检测率。
- **BU / U / U∂**：良性任务完成率 / 攻击下任务成功率 / 攻击下部分步骤（如只读子目标）完成率。
- **密码学延迟**：Ed25519签名验证与SHA‑256哈希链的微基准测试。

### 4. 资源与算力
文中**未提及具体的GPU型号、数量或训练时长**。原因在于NCS是一种**免训练（training‑free）** 的防御方法，不涉及模型微调，且对商业黑盒API兼容。所有的密码学操作均为轻量级CPU运算，因此无需讨论大规模算力资源。

### 5. 实验数量与充分性
论文进行了**约六类独立实验**，具备较好的覆盖度与客观性：
- **IPI攻击防御**：4模型 × 5防御配置（含基线），报告ASR、U等指标，并分析了弱模型上的残余风险。
- **针对FATH的绕过攻击**：2种攻击构造法，在QA和URL任务上测式两个模型，暴露纯文本标签防御的脆弱性。
- **参数劫持攻击**：3种骨干模型的详细ASR_args对比，揭示不同模型对参数篡改的敏感性差异（如GPT‑4o‑mini 100%被攻破）。
- **消融实验**：通过4种篡改场景（重排、后缀修改、追加块）证明仅有完整NCS‑Full才能强制执行安全策略。
- **密码学微基准**：1,000次迭代测量p50、p99延迟，证明开销可忽略。
- **形式化安全分析**：给出EU‑CSA安全性规约，证明指令真实性和控制流完整性。

各对比方法均在同一设定下复现，数据平衡，评估维度公平。唯一可能的不足是AgentDojo的样本总量未明确说明（原文未写具体数量），但在同类研究中具备代表性。

### 6. 论文的主要结论与发现
- **攻击防御效果显著**：NCS‑Full在几乎所有模型上将ASR_tool降至0%（除GPT‑4o‑mini残余8.3%，且该残余仅为自然语言输出泄露，非工具劫持）。参数劫持ASR_args全面降至0%。
- **良性效用保持良好**：在安全性提升的同时，良性任务完成率（BU）仅有轻微下降，部分情况下甚至因主动阻断恶意干扰而提高了攻击下的任务成功率（U）。
- **计算开销极小**：全套流验证延迟约1.2毫秒（均值），比单次LLM推理（数秒）低3个数量级以上，完全不影响代理响应时间。
- **安全范式转换**：NCS证明将LLM视为不可信意图生成器，并用密码学强制执行指令完整性和参数绑定，是应对智能体工作流安全威胁的有效路径。

### 7. 优点
- **架构创新**：首次将神经–符号计算原理系统性地应用于代理运行时安全治理，实现了权限的刚性分离。
- **状态化加密绑定**：流签名与逐块哈希链认证不仅验证指令来源，还强制绑定执行顺序和参数，从根本上阻止控制流劫持。
- **零训练、高兼容**：无需修改模型权重，可与商业API及标准密码接口（PKCS#11）集成，部署成本低。
- **安全可验证**：提供了形式化的安全定理和完整的失效审计追迹，满足法规遵从需求。
- **实验扎实**：涵盖多种攻击向量（IPI、参数劫持、对同类防御的绕过），对比基准全面，消融实验清晰揭示了各组件的必要性。

### 8. 不足与局限
- **功能范围有限**：当前NCS实现仅支持签名和哈希操作，尚不支持加密、阈值签名等更丰富的密码原语。
- **自然语言输出泄露未完全解决**：对GPT‑4o‑mini等弱模型，攻击者仍可能通过非工具调用的文本回复间接泄露信息（如8.3%残余ASR均为此类），NCS目前未过滤此类输出。
- **效用依赖模型规划能力**：在GPT‑4o‑mini上，BU和U均下降，因为模型缺乏将部分可执行子任务转化为完整任务成功的能力，说明NCS的保护可能放大弱模型的性能瓶颈。
- **策略集成不足**：虽提及与角色访问控制策略的结合，但未在原型中实现，未与组织级身份策略深度联动。
- **假设条件**：依赖安全通信信道、可信密码硬件边界，以及用户私钥得到妥善保护；这些假设的违背可能削弱系统安全性。
- **实验规模**：自定义数据集仅85个案例，可能存在场景偏差；未在更大规模、更多样化的真实企业工作流中评估。

（完）
