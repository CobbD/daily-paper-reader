---
title: "Trusted Credentials, Untrusted Behavior: Benchmarking LLM-Agent Security in High-Performance Computing"
title_zh: 受信任的凭证，不可信的行为：高性能计算中LLM代理安全性的基准评测
authors: Jie Li
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18485v1"
tags: ["query:agent-security"]
score: 9.0
evidence: 在高性能计算环境中对LLM智能体的对抗指令（提示注入）安全性进行基准测试
tldr: 大语言模型代理在高性能计算中按用户凭据执行任务，易受间接提示注入攻击，导致授权命令被恶意重定向。本文定义“被劫持的授权代理”威胁模型，系统分析HPC环境中调度器、共享存储等带来的攻击面。当前控制无法表达任务意图，需设计新安全机制。作者提出研究议程并计划构建TaskBound基准，以推动HPC代理安全评估。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1715, \"height\": 597, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18485v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 895, \"label\": \"Table\"}]"
motivation: HPC中LLM代理继承用户凭据，传统访问控制无法防止对抗指令对其行为重定向，造成安全风险。
method: 通过分析HPC典型工作流与架构，定义威胁模型，识别攻击面并评估现有控制不足，规划基准TaskBound。
result: 揭示了调度器、共享文件、多项目账户等独特攻击向量，以及现有身份隔离机制缺乏任务意图保护的问题。
conclusion: 呼吁为HPC代理开发任务绑定安全机制，并通过TaskBound基准实证验证防护方案，确保代理行为符合用户意图。
---

## 摘要
大语言模型（LLM）代理开始在高性能计算（HPC）中承担常规工作，包括监控 Slurm 作业、诊断构建失败、检查模拟输出以及协调科学工作流。为了完成这些工作，代理通常以其用户凭证运行，并继承用户对文件和调度器的访问权限。这种安排产生了一种普通账户级控制无法捕获的故障模式：日志、工具描述、共享文件或同伴代理消息中的对抗性指令可能将代理重定向至超出用户所分配任务的范围，即使最终执行的每条命令都经过认证且被该账户允许。我们将此称为“被劫持的授权代理”问题。现有的代理安全性研究解释了相关机制，如间接提示注入和工具滥用，但通常在网页、企业或个人助理环境中进行评估。相比之下，HPC 安全在身份和隔离方面拥有成熟的控制措施，但通常不体现特定任务的意图。本文定义了 HPC 环境中的威胁模型，识别了由调度器、共享存储、多项目账户和科学工作流产生的攻击面，并检查了当前控制措施的不足之处。最后提出了研究议程和一项名为 TaskBound 的实证基准测试计划。

## Abstract
Large language model (LLM) agents are starting to take on routine work in high-performance computing (HPC), including monitoring Slurm jobs, diagnosing failed builds, inspecting simulation output, and coordinating scientific workflows. To do this work, an agent commonly acts under its user's credentials and inherits the user's access to files and the scheduler. This arrangement creates a failure mode that ordinary account-level controls do not capture. Adversarial instructions in a log, tool description, shared file, or peer-agent message may redirect the agent beyond the task the user assigned, even though every resulting command is authenticated and permitted for that account. We refer to this as the hijacked authorized agent problem. Existing agent-security studies explain relevant mechanisms, such as indirect prompt injection and tool misuse, but generally evaluate them in web, enterprise, or personal-assistant settings. HPC security, by contrast, has mature controls for identity and isolation but does not ordinarily represent the intent of a particular task. This paper defines the threat model in the HPC setting, identifies attack surfaces created by schedulers, shared storage, multi-project accounts, and scientific workflows, and examines where current controls fall short. It concludes with a research agenda and a plan for an empirical benchmark, TaskBound.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）代理在高性能计算（HPC）环境中以用户凭据执行任务时，传统账户级访问控制无法防止来自日志、工具描述、共享文件或同伴消息中的对抗性指令对代理行为的重定向。该问题被称为“被劫持的授权代理”（hijacked authorized agent）。
- **整体含义**：现有的 HPC 安全机制（身份认证、权限隔离、资源配额）只能验证“谁在做什么”，无法表达和约束“一项特定任务允许做什么”。而当前 LLM 代理安全研究主要在网页、邮件、旅行等场景下进行，未考虑 HPC 特有的调度器、并行文件系统、多项目账户和科学工作流等资源与政策。因此，HPC 领域急需一种新的威胁视角和安全评估方法。
- **研究动机**：
  - LLM 代理已开始进入 HPC 实际工作流（如诊断作业、构建软件、协调科学工作流），且部分计算中心已公开提醒用户注意相关风险。
  - 现有两个相邻领域的工作存在“断层”：代理安全基准缺失 HPC 资源与政策，而 HPC 安全控制不评估对抗性内容对已验证代理的操控。
  - 论文旨在填补这一空白，定义威胁模型、识别攻击面，并提出研究议程和实证基准计划。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将研究重点从“账户权限”转移到“任务意图”，即通过界定代理被指派的任务边界，来识别即使所有操作均通过系统授权却偏离用户本意的攻击行为。
- **威胁模型定义**（The Hijacked Authorized Agent）：
  - 攻击发生在代理正常读取作业输出、工具描述、共享文件或同伴消息时，其中包含的恶意指令将其引向授权但非预期的操作。
  - 假设用户合法启动代理且代理拥有正确访问权限；攻击者仅具影响内容的能力（无需提权、修改模型权重或破解内核）；集群控制平面、调度器和基准环境可信。
  - 将攻击者能力划分为五类（C1–C5），分别对应投毒科学文件、影响调度器日志、控制工具描述/输出、写入共享文件系统状态、劫持同伴代理。
- **攻击面分类体系**（HPC 环境特有的五类攻击表面）：
  1. **共享并行文件系统投毒**：恶意内容潜藏在结果文件、缓存、README 中，持久化地影响后续代理。
  2. **调度器日志注入**：通过影响作业的标准输出/错误输出或记账记录，将有害文本送入代理的上下文，可能导致代理滥用配额（如重复提交任务）。
  3. **工具、模块和 MCP 投毒**：HPC 站点模块、构建助手脚本或 MCP 工具服务器的描述和输出可引导代理选择恶意工具或执行有害操作。
  4. **跨项目数据泄漏**：利用用户多项目身份，代理被从一个项目重定向至另一个项目的数据读取，并通过正常 API 请求外泄，账户权限不产生任何告警。
  5. **多代理协同窃密**：多个代理通过传递中间工件或消息实现协同攻击，单点日志难以检测整体攻击链。
- **现有控制不足分析**：通过对比表（Table 2）指出身份认证、POSIX 权限、配额、隔离、审计等控制无法记录任务意图和链接动作来源，缺失**任务范围许可、上下文来源追踪、出口控制、连接多个代理的横向关联**等关键能力。
- **安全需求与基准计划**：论文未提出具体算法或公式，而是明确了 HPC 代理平台应具备的七项安全能力（任务范围授权、上下文来源元数据、出口控制、文件系统卫生、工具信任检查、调度器感知的遏制、跨代理关联监控），并提出了一个仍在开发中的基准 **TaskBound**。该基准将模仿 AgentDojo 的做法，在 HPC 场景（Slurm 操作、项目范围文件策略、科学参数完整性检查）中同时测量任务效用和攻击成功率。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- 本文是一篇**立场/愿景论文**，明确声明“不包含实证评估”（not an empirical evaluation）。
- **未使用任何数据集**，也未进行任何实验对比。
- **基准设计思路**：文中介绍了计划构建的 **TaskBound** 基准，它将包含 Slurm 操作、项目作用域文件系统策略、科学参数与溯源完整性验证等 HPC 典型任务，并与对抗性上下文攻击配对设计。该设计借鉴 AgentDojo 的双维度评估（任务成功率 + 攻击成功率），但尚处于开发阶段。
- 因此，论文未提供实验结果，亦无具体的对比方法。

## 4. 资源与算力

- 全文未提及任何计算资源或算力配置（无 GPU 型号、数量、训练时间等信息），因为论文未进行实验训练或基准运行。

## 5. 实验数量与充分性

- **没有实验**。这是一篇威胁特征描述与研究议程文章，仅从概念上分析攻击面和提出安全需求。
- 充分的实证支持是缺失的，作者也承认本文的定位是“问题定义和规划”，未来的实证工作将交由 **TaskBound** 基准完成。

## 6. 论文的主要结论与发现

- **失败模式的新定义**：在 HPC 中，攻击者可通过内容操控使已验证代理执行偏离任务意图的授权操作，而无需突破传统安全机制。这一模式被系统化为“被劫持的授权代理”威胁模型。
- **HPC 环境存在独特且多样的攻击面**：调度器日志、共享文件系统、工具描述、多项目账户和多代理通信为提示注入类攻击提供了比通用 enterprise 场景更丰富的入口和更具破坏性的后果（如配额消耗、科学结果污染、跨项目数据泄漏）。
- **现有控制存在根本性缺失**：身份和权限安全无法体现任务意图，导致合法账户在恶意上下文驱动下的异常行为无法被有效监控或阻断。
- **研究方向的紧迫性和实用性**：在 LLM 代理已部署在 HPC 环境中的现实下，需要立即开展安全基准评估和防御机制构建，而非等待渐进修正。论文为 HPC 代理安全提出了结构化的研究议程。

## 7. 优点：方法或实验设计上的亮点

- **跨领域问题定义**：成功连接 LLM 代理安全和 HPC 系统安全这两个通常独立的研究领域，提出一个具体的、可验证的新问题。
- **威胁模型切合实际**：紧扣 HPC 特点（多项目账户、Slurm 调度器、模块/MCP 工具）构建攻击面分类，具有很强的实践针对性。
- **清晰的现状分析**：通过对比表格（Table 2）直观展示了现有控制与“任务意图保护”之间的差距，为后续系统设计提供明确目标。
- **延续性研究设计**：规划中的 TaskBound 基准直接参照已获认可的 AgentDojo 设计方法，兼具任务效用和安全评估，具备合理的可行性预期。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **完全缺乏实证验证**：论文未提供任何攻击成功率、任务效用数据或防御效果评估。所有主张仍是假说，待实现和测量。
- **实施可行性未讨论**：提出的安全需求（如任务范围授权、上下文出处、出口控制等）在实际 HPC 站点（通常难以修改核心调度器或文件系统）如何实现，未给予具体路线图。
- **攻击者能力设定较窄**：假设攻击者仅能影响内容，不考虑传统提权、模型权重篡改或社会工程等结合的混合攻击，可能低估实际风险。
- **任务意图的规范难题**：科学工作往往具有探索性，如何精确、弹性的指定“任务边界”是一个公认的难题，论文未深入探讨该形式化问题对安全策略可执行性的影响。
- **适用范围的声明范围**：文章以威胁描述和议程为主，对于希望即刻获得安全评分或对比实验结果的读者来说，提供的信息有限。
- **缺乏量化比较**：没有与已有的通用代理安全基准（如 AgentDojo）直接对比，无法明确 HPC 独特性在安全严重程度上的量化差异。

（完）
