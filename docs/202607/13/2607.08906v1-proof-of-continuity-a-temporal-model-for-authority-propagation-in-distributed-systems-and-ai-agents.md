---
title: "Proof-of-Continuity: A Temporal Model for Authority Propagation in Distributed Systems and AI Agents"
title_zh: 连续性证明：分布式系统与AI代理中权限传播的时序模型
authors: Nicola Gallo
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08906v1"
tags: ["query:agent-security"]
score: 9.0
evidence: 为AI代理和分布式系统中的执行链引入权限传播约束，以防止权限提升
tldr: 在分布式系统和AI代理中，现有基于持有（如令牌）的授权模型难以维持跨步骤执行的因果一致性，易引发混淆代理问题。为此，本文提出Proof-of-Continuity，一种基于Provenance Identity Continuity模型的权限传播规则，要求步骤间因果链接且授权非扩张传递，并设计了单跳因果原语Proof of Relationship来组合实现。该方案从模型层面消除了权限滥用，确保后续权限必定源自初始授权上下文，为复杂执行链提供了安全的授权传播基础。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08906v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1251, \"height\": 415, \"label\": \"Table\"}]"
motivation: 现有Proof-of-Possession模型仅靠持有凭证授权，未维护执行步骤间的因果链，在多服务或AI代理场景下存在安全缺陷。
method: 提出Proof-of-Continuity，通过Proof of Relationship原语的传递组合，强制执行步骤间因果链接与权限的非扩张传播。
result: 在Proof-of-Continuity下，混淆代理问题被根本消除，后续步骤权限严格受限于初始授权上下文，无法越权。
conclusion: Proof-of-Continuity补充了OIDC等认证技术，为分布式系统与AI代理提供了一种安全的因果授权传播模型。
---

## 摘要
持有证明（Proof-of-Possession）授权模型从拥有令牌、凭证或能力等工件中获取权限。本文认为，对于离散执行链（无论跨多个服务还是在同一机器内以分离步骤发生）而言，持有是不够的，因为它不能保证请求起源与后续步骤所行使权限之间的因果关系得以保留。我们引入了连续性证明（Proof-of-Continuity），这是源身份连续性（PIC）模型的一种最小权限传播规约。在该模型中，每个执行步骤必须与上一步骤具有因果联系，且只能传播从起源接收的权限的非扩展子集。我们引入了关系证明（Proof of Relationship），这是一个单跳因果原语，其传递组合构成连续性证明；这些对持有证明起补充而非替代作用。在该模型下，混淆代理问题无法被视为有效的模型行为：在后续步骤行使的任何权限必须已存在于起源权限上下文中。这直接关系到分布式系统和AI代理，在这些系统中，执行器在持有多个权限源的同时调用工具和下游服务，导致相同的权限/因果关系错配跨越服务边界反复出现。在连续性证明下，这些源可以一同携带，但绝不合并为一个组合权限，因为每个步骤仅根据导致它的那条世系的权限上下文进行授权。本文关注的是授权传播而非身份认证：OIDC、可验证凭证、钱包和工作负载身份等身份识别与认证机制依然是确立起源的补充机制，而连续性证明则解决起源确立后权限如何传播的问题。

## Abstract
Proof-of-Possession authorization models derive authority from the possession of artifacts such as tokens, credentials, or capabilities. This paper argues that possession is insufficient for discrete execution chains, whether they span multiple services or occur as separated steps within the same machine, because it does not guarantee preservation of the causal relationship between the origin of a request and the authority exercised at later steps. We introduce Proof-of-Continuity, a minimal authority-propagation discipline for the Provenance Identity Continuity (PIC) model, in which each execution step must be causally linked to the previous step and may only propagate a non-expansive subset of the authority received from the origin. It introduces Proof of Relationship, a single-hop causal primitive whose transitive composition is Proof-of-Continuity; these complement Proof-of-Possession rather than replace it. Under this model, the confused deputy condition cannot be satisfied as valid model behavior: any privilege exercised at a later step must already be present in the origin authority context. This is directly relevant to distributed systems and AI agents, where executors invoke tools and downstream services while holding multiple authority sources, so that the same authority/causality mismatch recurs across service boundaries. Under Proof-of-Continuity these sources may be carried together but are never merged into a combined authority, since each step is authorized only against the authority context of the lineage that caused it.   This paper concerns authorization propagation rather than authentication: identity and authentication mechanisms such as OIDC, verifiable credentials, wallets, and workload identity remain complementary mechanisms for establishing the origin, while Proof-of-Continuity addresses how authority propagates after that origin exists.

---

## 论文详细总结（自动生成）

```
# 论文总结：Proof-of-Continuity：分布式系统与AI代理中权限传播的时序模型

## 1. 论文的核心问题与整体含义
- **核心问题**：现有主流的**持有证明（Proof-of-Possession）**授权模型（如Bearer Token、JWT、Capability）仅依赖对“工件”的拥有来判定权限，无法保证**多步执行链中的因果连续性**。当执行链跨越多个服务或步骤时，后续步骤可能使用与原始请求无关的权限，从而引发经典的**混淆代理问题（Confused Deputy）**。
- **整体含义**：论文指出持有证明模型的本质缺陷——它丢失了**时间维度的世系（lineage）信息**，导致无法区分同一权限在不同因果链中的使用。作者提出的**连续性证明（Proof-of-Continuity, PoC）**正是为了补全这一缺失的时间轴，将授权从静态持有约束升级为动态因果传播约束，从根本上消除跨步骤越权。

## 2. 方法论：核心思想与技术细节
- **模型框架（PIC Model）**：定义**执行链π** 为一系列步骤序列，每一步携带一个**权限上下文Ci**（操作与资源的集合）。权限在传播过程中必须满足：
  - **因果连续性**：每一步通过**关系证明（PoR）**与上一步形成可验证的单跳因果链。
  - **单调非扩张性**： `Ci+1 ⊆ Ci`（或一般情况下的 `Ci+1 ⪯ Ci`），即后续步骤的权限必须是前一步的子集，不可新增权限。
- **PoR 与 PoC 的关系**：PoR 是单跳因果原语，其**传递组合**构成 PoC，从而全局保证 `Cn ⊆ C0`。
- **核心定理**：
  - **PIC 安全性定理**：若模型成立，则任何步骤都无法行使超出起源上下文 `C0` 的权限。
  - **投影定理**：忽略世系的持有策略无法区分具有相同权限但不同世系的事件，因此无法防御混淆代理。
  - **不可能性定理**：在有效的 PIC 执行中，混淆代理的条件无法被满足（权限缺失于 `C0` 则必然缺失于后续所有步骤）。
- **关键区别**：PoC 并不替代 PoP，而是与其互补；PoC 处理**授权传播**，而不涉及**身份认证**（后者仍由 OIDC、SPIFFE 等机制完成）。

## 3. 实验设计
- **实验属性**：本文为**纯理论/形式化安全模型论文**，**没有**进行任何代码实现或仿真实验。
- **验证方式**：通过形式化定义、定理证明和反例论证来展示方法的正确性与能力边界，例如：
  - 证明持有模型在授权混合（authority mixing）下必然允许混淆代理。
  - 证明 PoC 在放弃世系不变性后能够重构安全性。
- **对比方法**：文中未做实验对比，但在相关工作中将 PoC 与能力系统（Capability）、基于历史的访问控制、Maccaroon、DPoP 等模型进行了概念性对比。

## 4. 资源与算力
- **算力需求**：**无**。论文未涉及模型训练、大规模仿真或密码学原语部署，因此未提及任何 GPU、训练时长或算力消耗。

## 5. 实验数量与充分性
- **实验数量**：由于是理论论文，没有实验组数、消融实验等概念。
- **充分性评价**：论文以**严格的数学形式化**来支撑其论点，给出了 6 个定义、5 个定理、1 个引理和 1 个推论，逻辑链条封闭且自洽。从理论安全模型的角度看，论证是充分的；但缺少**实际工程验证**（如原型实现、性能开销、广域网环境测试等）。

## 6. 主要结论与发现
- **授权是时序连续属性**：权限不应被看作主体“拥有”的静态物，而应是执行链上可追溯的**因果延续**。
- **PoC 根除混淆代理**：在 PoC 下，任何后续步骤的权限必须已存在于起源上下文，混淆代理成为模型内不可能的状态。
- **持有证明的不足**：单纯的 PoP 是授权关系的空间投影（遗忘世系），对于多跳执行链本质上是不完整的；所有弥补该缺陷的工程手段（如 nonce、audience 绑定）本质上都隐式引入了世系信息，即走向连续性感知。
- **适用性**：该模型直接适用于 AI 代理、跨服务调用链等场景，能防止因提示注入或参数篡改导致的非授权操作。

## 7. 优点
- **理论洞察深刻**：用投影语言精确定义了持有模型丢失时间维度的缺陷，将混淆代理重新解读为“世系碰撞”。
- **模型最小化且可组合**：PoR/PoC 提供了干净的单跳/多跳原语，不影响底层身份认证机制，与现有体系互补。
- **安全属性构建式保证**：通过单调子集传播，直接获得 origin-bounded 安全，而不是依赖运行时的检查策略。
- **场景相关性**：明确将 AI 代理的越权问题纳入框架，具有现实前瞻性。

## 8. 不足与局限
- **未给出 PoR 的具体构造**：PoR 的实现（加密关系证据）被声明为未来工作，当前模型仅停留在抽象层面，无法直接指导实现。
- **线性链假设**：当前模型仅处理线性执行链，未覆盖分叉、汇合、DAG 等复杂拓扑。
- **授权撤销未建模**：没有定义世系中途失效或权限撤销的机制。
- **策略翻译未解决**：异构服务间权限词典的翻译（T_{i→i+1}）被认为是外部策略输入，其正确性验证和生成被留作开放问题。
- **缺少实证评估**：没有原型系统或性能数据，难以评估在真实分布式系统中的开销、部署复杂度及对延迟的影响。
- **依赖可信执行环境或额外信任根**：关系证明的不可伪造性假设可能引入新的信任假设。

（完）
```
