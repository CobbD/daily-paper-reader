---
title: A Dual-CRDT Architecture for Decentralized Trust Governance and Evolution
title_zh: 一种用于去中心化信任治理与演进的双CRDT架构
authors: Amos Brocco
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06068v1"
tags: ["query:byzantine"]
score: 9.0
evidence: 双CRDT架构用于拜占庭容错系统中的去中心化信任治理
tldr: 去中心化复制场景中，CRDT实现最终一致性，但拜占庭容错需识别可信更新，其依赖的信任信息常外置于复制状态，难以协同演化。本文提出双CRDT架构：Trust CRDT存储并演化治理数据（如成员权限、信任策略），Data CRDT基于其确定性重建并执行拜占庭过滤，生成可信视图。该设计使治理规则成为可复制状态，支持通过常规CRDT操作动态修改，实现递归治理——治理规则既管控应用数据，又规定自身变更流程。原型在Melda/melda-sec上实现，验证了内置信任治理与自我演进的可行性，为拜占庭容错CRDT系统的去中心化自治提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06068v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1111, \"height\": 832, \"label\": \"Figure\"}]"
motivation: 在拜占庭容错CRDT系统中，信任决策所需的信息（如成员身份、信任规则）通常管理在复制状态之外，无法随系统演化而灵活调整，限制了去中心化治理的适应性。
method: 本文提出由Trust CRDT和Data CRDT构成的双层架构，将治理信息内化为可复制状态，通过确定性重建和拜占庭过滤实现信任感知的数据视图生成。
result: 形成递归治理模型，治理规则可通过普通CRDT更新自行演化，同时控制应用数据的重建，原型在Melda和melda-sec上实现。
conclusion: 该架构为去中心化信任治理与演化提供了可行方案，增强了拜占庭容错CRDT系统的自治性和适应性，但作为初步探索，仍需进一步研究。
---

## 摘要
虽然CRDT提供了去中心化的复制和最终一致性，但拜占庭容错部署需要一种机制来决定哪些更新应被信任，从而贡献到重建的状态。在实践中，这些决策背后的信任关系可能随着参与者的加入或离开、身份变更以及治理规则的修订而随时间演变。然而，用于做出信任决策的信息通常在复制状态之外进行管理。本文引入了一种由信任CRDT和数据CRDT组成的双CRDT架构。信任CRDT存储并演化治理信息，而数据CRDT根据从信任CRDT导出的信任配置进行重建。因此，治理成为复制状态，而非外部管理的人工制品。基于确定性重建和拜占庭信任过滤，所提出的模型允许信任关系和治理规则通过普通的CRDT更新进行演化。由此产生的架构提供了一种递归治理模型，其中治理规则决定了其自身的未来演化，同时治理应用数据。该方法在Melda和melda-sec之上实现为原型，并应被视为对拜占庭容错CRDT系统的去中心化信任治理与演化的一次初步探索。

## 速览
**TLDR**：在拜占庭容错CRDT系统中，信任管理常游离于复制状态之外，缺乏灵活演化能力。本文提出双CRDT架构，将治理逻辑纳入Trust CRDT作为一等复制实体，Data CRDT依赖其信任配置进行确定性状态重建。基于Melda的原型实现展示了递归治理：治理规则既可控制应用数据，又能自演化。该工作为去中心化信任治理和演化提供了初始概念验证。 \
**Motivation**：现有拜占庭容错CRDT系统的信任治理信息位于复制状态外，难以随系统演化而灵活更新。 \
**Method**：提出Trust CRDT和Data CRDT分离的双CRDT架构，治理数据本身作为复制状态更新，驱动数据状态的重建。 \
**Result**：原型在Melda实现，验证架构支持递归治理，治理规则可自演化并同时控制应用数据。 \
**Conclusion**：为去中心化信任治理提供新范式，将治理规则内化于复制状态，实现递归式演化管理。

---

## Abstract
While CRDTs provide decentralized replication and eventual consistency, Byzantine-resilient deployments require mechanisms for deciding which updates should be trusted and therefore contribute to the reconstructed state. In practice, the trust relationships underlying these decisions may evolve over time as participants join or leave, identities change, and governance rules are revised. However, the information used to make trust decisions is typically managed outside the replicated state itself. This paper introduces a dual-CRDT architecture composed of a \emph{Trust CRDT} and a \emph{Data CRDT}. The Trust CRDT stores and evolves governance information, while the Data CRDT is reconstructed according to the trust configuration derived from the Trust CRDT. Governance therefore becomes replicated state rather than an externally managed artifact. Building upon deterministic reconstruction and Byzantine trust filtering, the proposed model allows trust relationships and governance rules to evolve through ordinary CRDT updates. The resulting architecture provides a recursive governance model in which governance rules determine their own future evolution while simultaneously governing application data. The approach is implemented as a prototype on top of Melda and melda-sec and should be viewed as an initial exploration of decentralized trust governance and evolution for Byzantine-resilient CRDT systems.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：冲突无关复制数据类型（CRDT）可在去中心化环境下实现强最终一致性，但在拜占庭（恶意）环境中，副本必须判断哪些更新值得信任才能参与状态重建。这种信任决策通常依赖外部的静态信任配置（如可信身份、授权规则、背书策略等）。
- **核心问题**：在长期运行的动态去中心化系统中，信任关系会随时间演化（参与者加入/离开、密钥轮换、规则修订等），但现有的信任配置往往通过带外管理或集中式基础设施分发，难以与系统状态同步演化。此前的研究虽然提出了确定性重建和细粒度信任过滤机制，却未解决“信任配置本身如何被建立、分发和演化”的问题。
- **整体含义**：本文提出将治理信息（信任配置）本身作为可复制的系统状态，通过双CRDT架构实现去中心化的信任治理与演化，使治理规则既能管控应用数据，又能递归地控制自身的变更，从而消除对外部管理的依赖。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：将治理与数据分离为两个独立的CRDT——**Trust CRDT** 存储并演化治理文档（含可信身份、角色、授权策略、背书模式、白名单/黑名单等），**Data CRDT** 存储应用数据。重建过程分两步：
  1. 先根据引导信任配置重建 Trust CRDT，得到治理文档；
  2. 从治理文档派生出信任配置，用于重建 Data CRDT。
- **递归治理机制**：治理文档包含两个信任规则集，一个管理 Trust CRDT 自身的未来演化，另一个管理 Data CRDT。治理更新（如增删可信身份、修改授权策略、加黑/白名单等）被视为普通的 CRDT 更新，但必须遵守当前治理规则所定义的背书和授权要求。因此，治理规则决定了其自身的演化路径，形成递归的自我治理。
- **信任执行组件（TrustAdapter）**：
  - **可信身份**：基于公钥密码学，将身份与网络地址解耦。
  - **背书模式**：支持放任（permissive）、单一背书、多数背书（majority）、全体背书（unanimous）等，决定一个更新在多少可信身份签名后被接受。
  - **授权策略**：基于对象标识符（object identifiers）和角色进行精确的写权限控制，默认为“全拒绝”，支持严格写入模式。
  - **内容级信任**：通过白名单/黑名单机制对特定 delta 块直接放行或屏蔽，实现细粒度的内容信任，不因身份变化而殃及池鱼。
- **不动点重建过程**：副本先用一个初始（bootstrap）信任配置重建 Trust CRDT，获取治理文档；再用该文档派生的信任配置重新扫描 Trust CRDT 的历史，如此反复迭代，直到没有新的治理变更生效（到达不动点）。随后用最终信任配置重建 Data CRDT。若未来信任配置改动导致历史治理更新不再合规，可通过白名单机制显式保留那些必需的历史决策。
- **收敛性依赖**：Trust CRDT 和 Data CRDT 均为普通 CRDT，其收敛性由底层确定性重建框架保证。副本可能因观察到的治理更新不同而短暂产生不同的信任视角，但一旦接收到相同的治理与数据更新集合，便会收敛到相同的应用状态。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- 论文没有提供具体的实验评估。作者明确指出该工作属于**早期探索**（“work in progress”），原型在 Melda 与 melda‑sec 库上实现，但并未描述数据集、测试场景、benchmark 或对比方法。全文属于系统架构与模型描述，而非实验验证型论文。

### 4. 资源与算力
- 文中**未提及任何算力信息**（如 GPU 型号、数量、训练时长等）。因其为分布式系统架构论文，不涉及模型训练或高强度计算，不适用此类资源描述。

### 5. 实验数量与充分性
- 文中**无定量实验**。未列出不同条件下的实验组、消融实验或对比研究。论文以阐述架构和机制为主，实验覆盖缺失，距离充分、客观的验证尚有较大距离，作者也自认是初步尝试，有待进一步工作。

### 6. 论文的主要结论与发现
- 将治理视为可复制的状态（Trust CRDT）是可行的，信任配置不再需要外部管理。
- 双CRDT架构实现了**递归治理**：治理规则可以在不破坏系统收敛性的前提下决定自身的演化。
- 信任变化（身份变更、策略更新、内容撤销等）通过普通的 CRDT 更新自然传播，无需集中式协调。
- 历史治理决策可通过白名单保留，避免因规则演化而造成的循环失效。
- 整个架构继承了底层确定性重建的收敛保证，不引入额外的一致性假设，仅在临时不一致后最终达成共识。

### 7. 优点
- **治理内生化**：将信任配置从外部静态文件转变为动态、自管理的复制状态，适应不断变化的去中心化环境。
- **递归自指性**：治理规则既管理应用数据又管理自身演化，实现了自我参照的治理闭环。
- **细粒度信任控制**：分离身份信任与内容信任，支持黑名单/白名单，实现对有害内容的精准剔除而不牵连身份。
- **机制通用性**：信任适配器、背书模式和授权策略是组合式的，可适用于不同 CRDT 家族，不限于 Melda 平台。
- **收敛保证继承**：未引入新的收敛障碍，收敛属性由已有确定性重建框架提供。

### 8. 不足与局限
- **缺乏形式化与证明**：递归治理模型、不动点性质、收敛性等均未给出形式化定义或证明，缺少拜占庭场景下的安全性分析。
- **实验缺失**：无任何性能、可扩展性或鲁棒性实验，无法评估原型在真实场景中的表现。
- **平台依赖原型**：当前原型构建于 Melda/melda‑sec 之上，虽声称概念通用，但缺乏在其他 CRDT 库或框架上的验证。
- **暂时不一致问题**：副本可能因观察到不同治理更新而短暂出现不同信任视角，虽然最终会收敛，但未分析临时分叉对上层应用的影响。
- **安全性边界未明**：在大量拜占庭行为（如恶意治理提案、密钥泄露、分裂的治理历史等）下，系统的实际韧性未经验证。
- **仅初步探索**：作者明确表示工作处于初期，许多设计选择尚未经过深入检验，距离实际部署仍较远。

（完）
