---
title: "Continual Learning With Participation Privacy: An Auditable Buffering-Aggregation Recipe"
title_zh: 持续学习中的参与隐私：一种可审计的缓冲聚合方案
authors: "T-H. Hubert Chan, Elaine Shi, Mengshi Zhao, Mingxun Zhou"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07209v1"
tags: ["query:fl-security"]
score: 8.0
evidence: 为联邦学习中的参与式隐私提供缓冲方法
tldr: "联邦学习等持续学习系统中，模型持续更新发布，需保护轨迹级参与隐私，但用户增删改变后续所有更新，传统汉明距离差分隐私分析不再适用。本文提出可审计缓冲-聚合配方，以随机缓冲包装将单编辑流转化为汉明式更新流，并结合认证定理将非自适应DP证明提升至自适应输入。结合树前缀和等标准原语，实现轨迹级(ε,δ)-DP，并建立隐私参数U与延迟的显式联系。该方案为持续学习参与隐私提供模块化、可审计的隐私-延迟权衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07209v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1706, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07209v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 521, \"height\": 452, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07209v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1144, \"height\": 576, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07209v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 957, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07209v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 536, \"height\": 273, \"label\": \"Table\"}]"
motivation: 持续学习中用户增删改变所有后续更新，致使传统汉明距离差分隐私分析失效。
method: 提出随机缓冲包装和认证定理，将单编辑流转化为汉明式更新流，并确立非自适应DP证明向自适应输入提升的条件。
result: "结合树前缀和等原语实现轨迹级(ε,δ)-DP，隐私参数U显式联系延迟，提供可审计的隐私-延迟权衡。"
conclusion: 为持续学习参与隐私提供模块化可审计方案，隐私-延迟权衡由参数U显式表征。
---

## 摘要
现代联邦学习和流式学习系统经常发布中间模型，因此隐私必须在自适应交互下对整个轨迹成立。受参与隐私的启发，我们研究了单编辑相邻用户流，其中一次插入/删除会改变所有后续更新，从而使标准的汉明邻居持续释放分析失效。我们给出了一种可审计的模块化方案。一个随机缓冲包装器生成大小为 [U, 2U] 的批次，将单编辑流简化为具有显式积压/延迟保证的汉明风格每批次更新流，其中 U 由隐私参数 (ε, δ) 校准。然后我们证明了一个认证定理，确定了何时针对持续原语的非自适应汉明邻居差分隐私证明可以提升到自适应输入：原语必须使用每轮新鲜的随机性，并在共同的自适应上下文下具有稳定的一轮隐私剖面。这些要素共同使用标准原语（例如树状前缀和），为单编辑流提供了轨迹级别的 (ε, δ)-差分隐私，并通过 U 建立了明确的隐私-延迟联系。

## 速览
**TLDR**：现代联邦与流式学习系统需在持续发布模型时保护参与用户隐私，但现有汉明邻近分析难以应对单编辑流中的级联偏移效应。本文提出可审计的缓冲聚合框架，利用随机缓冲将单编辑流转化为汉明式更新流，并证明在新鲜随机性及稳定性条件下非自适应DP可提升至自适应场景。结合树前缀和等原语，实现轨迹级(ε,δ)-差分隐私，并建立隐私参数U与系统延迟的显式关联，为隐私保护的持续学习提供了实用模块化方案。 \
**Motivation**：持续学习系统发布中间模型，参与隐私需保证全轨迹安全，但单编辑流破坏了相邻性。 \
**Method**：提出随机缓冲包装将单编辑流转化为汉明式每bin更新流，并证明认证定理：非自适应DP在新鲜随机性和稳定隐私下可提升至自适应。 \
**Result**：结合树前缀和等标准原语，实现轨迹级(ε,δ)-DP，并建立隐私参数U与延迟的显式联系。 \
**Conclusion**：该模块化方案为持续学习提供可审计的参与隐私保障，并量化隐私-延迟权衡。

---

## Abstract
Modern federated and streaming learning systems often release intermediate models, so privacy must hold for the full trajectory under adaptive interaction. Motivated by participation privacy, we study single-edit neighboring user streams, where one insertion/deletion shifts all subsequent updates and defeats standard Hamming-neighbor continual-release analyses. We give an auditable modular recipe. A randomized buffering wrapper emits bins of size $[U,2U]$, reducing single-edit streams to a Hamming-style per-bin update stream with explicit backlog/delay guarantees, where $U$ is calibrated by the privacy parameters $(\varepsilon,δ)$. We then prove a certification theorem identifying when a non-adaptive Hamming-neighbor DP proof for a continual primitive lifts to adaptive inputs: the primitive must use fresh per-round randomness and have a stable one-round privacy profile under common adaptive context. Together, these ingredients yield trajectory-level $(\varepsilon,δ)$-DP for single-edit streams using standard primitives (e.g., tree prefix sums), with an explicit privacy--latency link via $U$.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
*   **研究背景**：现代联邦学习与流式学习系统持续发布中间模型，攻击者可观察完整的参数轨迹。因此隐私保护必须覆盖全轨迹，且需应对自适应的交互（未来事件可能依赖已发布的模型）。
*   **核心问题**：传统的差分隐私（DP）分析大多基于**汉明邻近**（Hamming-neighbor）数据流（仅一个时间点取值不同）。但面向**参与隐私**（用户是否参与训练本身即为敏感信息）时，恰当的定义是**单编辑邻近**（single-edit neighbor）：两个流由另一个流通过插入或删除一个用户事件得到。这会打乱所有后续位置的对齐，使汉明邻近分析失效。
*   **整体含义**：本文为上述持续学习参与隐私场景提供一个模块化、可审计的解决方案，并显式建立了隐私预算（ε,δ）与系统延迟（缓冲大小U）之间的量化联系。

## 2. 论文提出的方法论
### 2.1 核心思想与模块化管道
提出“缓冲-聚合”管道，将问题分解为两个独立可审计的模块：
1.  **随机缓冲包装器（RandBin）**：将单编辑流转化为汉明式的按批次（bin）更新流，并为每个批次的尺寸和延迟提供确定性的界。
2.  **经认证的持续差分隐私原语**：对汉明式更新流进行私有聚合（如树状前缀和），并通过**认证定理**证明其在自适应交互下仍满足DP。

### 2.2 关键技术细节
*   **RandBin 缓冲机制**：
    *   参数：隐私预算 (ε<sub>b</sub>, δ<sub>b</sub>) 决定缓冲级别 `U = Θ(1/ε<sub>b</sub> · log(1/δ<sub>b</sub>))`。
    *   行为：维护FIFO队列，每步随机决定是否弹出一批数据，每批大小在 [U, 2U] 内随机，未被弹出的数据积压。
    *   效果：将单次插入/删除转变为最多影响连续两个批次内容的汉明式扰动。同时给出了积压（延迟）的确定上界：`O( (1/ε<sub>b</sub>) · log t · (log t + log(1/δ<sub>b</sub>)) )`。
*   **认证定理（Certification Theorem）**：
    *   条件：持续DP原语须满足 (a) 每轮使用独立的新鲜随机性；(b) 具有“稳定-上下文”性质（共同聚合上下文不改变单轮隐私代价），形式化为“定量可分解性”。
    *   结论：若满足上述条件，则对汉明流有效的**非自适应**DP证明，在自适应交互下依然成立。树状前缀和、下三角矩阵机制等标准原语均满足该条件。
*   **端到端DP-SGD实例化**：
    *   当 RandBin 发射一个批次时，用批内数据的裁剪梯度均值作为一步更新，送入经认证的持续DP聚合器（如私有前缀和），得到私有模型轨迹。总隐私为 (ε<sub>b</sub>+ε<sub>a</sub>, δ<sub>b</sub>+δ<sub>a</sub>)。

### 2.3 算法流程（文字说明）
1. 设置总隐私目标 (ε,δ)，分配 (ε<sub>b</sub>,δ<sub>b</sub>) 给缓冲，(ε<sub>a</sub>,δ<sub>a</sub>) 给聚合。
2. 根据 ε<sub>b</sub>,δ<sub>b</sub> 计算缓冲等级 U，初始化 RandBin。
3. 对于每步到来的事件 f<sub>t</sub>：
   * 送入 RandBin，若输出为 ⊥ 则不更新模型；若输出为批次 B<sub>t</sub>，则计算该批裁剪梯度均值 g<sub>k</sub>。
   * 将 g<sub>k</sub>（乘以学习率后的更新 u<sub>k</sub>）送入经认证的持续DP聚合器，得到私有前缀和 S<sub>k</sub>，并发布模型 w<sub>t</sub> = w<sub>0</sub> - S<sub>k</sub>。
4. 输出全轨迹模型序列及释放时间。

## 3. 实验设计
*   论文正文明确指出相关实验发表于另一篇会议论文 [CSZZ26]（同作者的 ICML 2026 版本）。本稿件未描述具体数据集、基准或对比方法。
*   文中仅概括说明会测量不同 (ε,δ) 下的模型准确率/损失，以及由 U 决定的经验延迟分布，并提到聚合器可选树机制或下三角矩阵机制。

## 4. 资源与算力
*   文中未提及任何 GPU 型号、数量、训练时长或硬件配置。实验部分完全缺失，无法评估算力需求。

## 5. 实验数量与充分性
*   由于本稿件本身**未包含任何实验**，无法评估实验数量、充分性、客观性或公平性。所有实证结论均需参考外部文献 [CSZZ26]。
*   从理论角度，作者提供了形式化的效用界（延迟、隐私），并通过引理、定理和认证清单（Checklist）构建了较完整的理论框架，逻辑上自成体系。

## 6. 论文的主要结论与发现
*   **轨迹级隐私保证**：通过 RandBin + 经认证的持续DP原语的模块化组合，可在单编辑邻近、自适应反馈下为全轨迹提供 (ε,δ)-DP。
*   **隐私-延迟显式联系**：缓冲级别 U 由隐私参数决定，并直接制约系统最大积压与事件包含延迟，给出了确定性的 `O(poly(log t)/ε)` 级别延迟界。
*   **可审计的适配性**：认证定理给出了可查验的条件清单，允许将一大批标准非自适应汉明DP原语安全地复用于自适应持续学习环境，无需为每个原语重新设计自适应证明。
*   **模块化视角**：将编辑-汉明-邻居转化与自适应安全分解为两个可独立分析的组件，简化了设计与分析。

## 7. 优点
*   **模块化与可审计**：清晰的组件分离（RandBin、认证DP原语）及可查验的认证条件，增强了方案的可信度与复用性。
*   **理论基础扎实**：形式化了参与隐私下的持续学习问题，提供了配套的NPDP、定量可分解性等定义，证明严谨。
*   **隐私‑延迟权衡显式化**：给出延迟的确定上界，并将延迟与隐私参数直接关联，对实际部署有指导意义。
*   **兼容标准原语**：树状前缀和、下三角矩阵机制等常用工具经简单审计即可兼容，降低实现门槛。

## 8. 不足与局限
*   **无实验支撑**：作为一篇理论型论文，完全缺失实证评估，无法检验实际效用与隐私‑延迟权衡的真实表现。
*   **延迟的对数因子**：延迟界包含 `log t` 因子，随时间缓慢增长，可能对极长时域的系统造成累积陈旧问题。
*   **近似DP的依赖**：延迟的确定上界依赖于 RandBin 使用截断几何噪声（近似DP），无法推广到纯DP（ε,0）设定并获得有限延迟。
*   **单编辑定义的局限**：仅考虑了单个事件级别的参与隐私，扩展到k‑跳编辑需要组隐私组合，会在隐私预算和延迟间引发更大的代价。
*   **认证条件的适用范围**：认证定理要求的定量可分解性虽然常见，但某些复杂的自适应优化器（如带有状态动量）需额外审计，文中未提供通用方法。
*   **多项附加假设**：分析需用户事件可鉴别（唯一ID），且输入值为离散范围等，这些在特定场景可能不成立。

（完）
